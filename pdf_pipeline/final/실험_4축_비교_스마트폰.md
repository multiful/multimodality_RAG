# 4축 비교 실험 — docling only / MinerU only / 고도화 파이프라인 / 베이스라인

**대상 PDF**: `pdf_pipeline/reference/SmartPhone/20260629_industry_47868000.pdf`
「스마트폰 수요 우려는 예견된 수순」 — 반도체 및 소부장 Weekly, 하나증권, 2026.6.29, Analyst 김록호. 31페이지.

**실험일**: 2026-07-23 · **환경**: Windows 11, CPU 전용(torch CPU), 재현 스크립트 `pdf_pipeline/final/work/`

**목표**: 같은 PDF 하나를 네 가지 방식으로 파싱→청킹→검색(RAG)했을 때 **지연시간**과 **정확성(구조/검색/페이지분류/라우팅)** 을 비교. 생성(LLM 답변) 단계는 4축 모두 같은 모델을 쓸 것이므로 이번 비교에서 **제외**(사용자 지침).

---

## 0. 이 문서가 어떤 PDF인가 (결과 해석의 전제)

먼저 대상 문서의 성격을 못 박아야 결과가 오독되지 않는다.

- **전 페이지가 디지털 텍스트 레이어를 가진 "쉬운" PDF** (스캔 이미지 아님). PyMuPDF `get_text()`가 페이지를 **행(row) 순서 그대로** 읽어낸다 — 표조차 컬럼이 뒤섞이지 않고 `종목 → 그 종목의 값들` 순으로 나온다.
- **표·차트가 지배적인 문서**: 밸류에이션/실적표 **표 1–11**(p1–6), **도표 1–93**(p7–22, 벡터 차트), **삼성전자·SK하이닉스 사업부문별 실적추정표**(p23–31).
- 이미지(래스터)는 p1의 헤더 로고 2개뿐. 차트는 전부 **벡터 드로잉 + 축 라벨 텍스트**.

→ 결론적으로 이 문서에서는 **"원문 답 추출(coverage)"은 어떤 파서든 대체로 잘 된다**. 진짜 변별력은 **① 출력 구조의 기계가독성, ② 지연시간, ③ 페이지 분류·라우팅** 에서 갈린다. 이 점을 염두에 두고 읽어야 한다.

---

## 1. 실험 설계

### 1.1 4개 축(axis) 정의

| 축 | 파서 | 청킹 | 표 처리 |
|---|---|---|---|
| **baseline** (고도화 전) | PyMuPDF `get_text()` 원문 | 페이지 단위(구조 인식 X) | pdfplumber `extract_tables()` 나이브 |
| **docling only** | docling `DocumentConverter` → markdown | 마크다운 블록(표 행 보존) | docling TableFormer(파이프표) |
| **MinerU only** | MinerU 3.4 CLI(pipeline) → markdown | 마크다운 블록(표 행 보존) | MinerU(HTML `<table>`) |
| **enhanced** (고도화) | YOLOv11n 페이지분류 → PyMuPDF 텍스트(정제) + TATR/pdfplumber 표 | Rule-based 계층 청킹(section_path) | Adaptive Router + TATR + canonical 매칭 |

- **enhanced**는 ERD/머메이드의 설계 그대로: `페이지 분류(YOLO) → 난이도 라우팅 → 텍스트(정제+계층청킹) / 표(TATR+canonical) → Hybrid 검색`. 구조화출력(OpenAI gpt-4o-mini)과 이미지 VLM 브랜치, Supabase 저장은 **파싱 품질 분리·공정성**을 위해 이번 비교에서 껐다(모두 선택 단계이며 생성/저장 계열).
- 4축 모두 **검색 백본을 동일하게 고정**: 팀 파이프라인의 실제 검색기 — **BGE-m3-ko dense(0.7) + BM25(0.3) min-max 가중합**(`text_processing/index_text.py`와 동일). 임베딩·검색을 고정해야 **파싱 품질만** 분리 측정된다.

### 1.2 정답셋 (직접 제작)

`pdf_pipeline/final/ground_truth_smartphone.json` — 원문에서 직접 검증한 **30문항** + 페이지 타입 라벨 + 구조 기대치.

| 유형 | 문항수 | 예시 |
|---|---|---|
| text_thesis (본문 서술) | 12 | Micron DRAM 상승률 60%↑, 코스피 -7.1%, Arm -24%, 비중확대 |
| valuation_table (표 1–2) | 8 | SK하이닉스 26F PER=8.7, Nvidia 시총 4,659,226, TSMC 26F P/E=27.6 |
| finance_table (표 p23) | 5 | 삼성전자 반도체 26F 매출 534.9조, Memory 505.1, DRAM 479.0 |
| chart_title (도표) | 5 | 도표 2 DDR5 16Gb 현물/고정가, 도표 90 글로벌 PC 출하량 |

### 1.3 채점 지표 (핵심)

- **추출 커버리지 (extraction coverage, 근접성)**: 파서가 뽑은 **전체 텍스트**에서 정답 라벨과 값이 **같은 위치 근처(정규화 220자 이내)** 로 붙어 있는가. → **청크 수와 무관한 "파싱 품질" 지표.** 표 셀 정렬이 무너져 값이 엉뚱한 곳으로 흩어지면 실패한다.
- **검색 hit@k (retrieval, 근접성)**: 질문으로 top-k 청크를 뽑았을 때 그 안에 근접-정답 청크가 있는가. → **파싱+청킹+검색 end-to-end** 지표.
- 값 매칭은 공백/천단위콤마/`%` 정규화 후 수행. 라벨명(기업/지수)과 짝지어 오매칭 방지.

---

## 2. 결과 요약 (한 장)

| 축 | 파싱 지연 | 청크 수 | **추출 커버리지** | **검색 hit@5** | 구조화 표 | 페이지분류 | 라우팅 |
|---|---:|---:|---:|---:|---|---|---|
| **baseline** | **0.16s** (표까지 8.5s) | 55 | **1.00** | **0.93** | ✗ (평면 텍스트) | 없음 | 없음 |
| **enhanced** | 47.6s | 78 | 0.90 | 0.87 | ✓ 447 canonical rec | **table F1 1.00 / chart F1 0.90** | p31 1건 오라우팅 |
| **docling only** | 401.6s | 673 | 0.97 | 0.83 | ✓ 31 표(파이프) | 없음 | 없음 |
| **MinerU only** | 236.5s | 306 | 0.90 | 0.80 | ✓ 34 표(HTML) | 없음 | 없음 |

> 지연시간(모두 CPU): **baseline ≪ enhanced(47.6s) ≪ MinerU(236.5s) ≪ docling(401.6s)**.
> 검색 hit@5는 청크 수 차이(=희석)에 영향을 받는다(§4.2). 희석을 보정한 **hit@10** 기준 순위는 **baseline 1.00 ≥ docling 0.97 > enhanced 0.87 ≥ MinerU 0.83**(§4.4). 청크와 무관한 **추출 커버리지**(baseline 1.00 / docling 0.97 / enhanced·MinerU 0.90)가 순수 파싱 품질에 가장 가깝다.

---

## 3. 지연시간 (빠르게 뽑는가)

| 축 | 총 파싱 | 세부 |
|---|---:|---|
| baseline | 0.16s (텍스트) | 나이브 표까지 포함 시 8.5s (pdfplumber) |
| **enhanced** | **47.6s** | 모델로드 0.4s + 페이지분류 6.1s + 텍스트 11.9s + 표(TATR) 29.5s |
| MinerU only | 236.5s | 전 31p 레이아웃+OCR 일괄 |
| docling only | 401.6s | 전 31p 변환(레이아웃+TableFormer+OCR) |

**핵심**: docling/MinerU "only"는 **31페이지 전부에 무거운 파싱을 통째로** 건다. 고도화 파이프라인은 **페이지 분류로 라우팅**해서 쉬운 페이지는 YOLO+pdfplumber+TATR(가벼움)로 처리 → 같은 문서를 **docling 대비 8.4배, MinerU 대비 5.0배 빠르게** 끝냈다. 이 문서엔 진짜 "어려운" 페이지가 없어 MinerU 호출은 1건(p31, 오탐)뿐이었고 나머지는 전부 로컬 경량 경로로 처리됐다.

---

## 4. 검색·추출 정확성 (검색 잘 하는가)

### 4.1 유형별 검색 hit@5 (근접성)

| 축 | text_thesis | valuation | finance | chart_title | **평균** |
|---|---:|---:|---:|---:|---:|
| baseline | 0.92 | 0.88 | **1.00** | 1.00 | **0.93** |
| enhanced | 0.83 | **1.00** | 0.80 | 0.80 | 0.87 |
| docling | 0.92 | 0.63 | 0.80 | 1.00 | 0.83 |
| MinerU | **1.00** | 0.88 | **0.00** | 1.00 | 0.80 |

### 4.2 왜 baseline이 검색에서 가장 높게 나오나 — 두 가지 이유

1. **이 PDF가 깨끗해서**다. PyMuPDF가 표를 행 순서대로 읽어 `SK하이닉스 … 8.7`이 원문에서 이미 인접한다 → 추출 커버리지 **1.00**(전 문항 통과). 즉 *답이 원문에 잘 남아있다*는 뜻이지, *구조가 좋다*는 뜻이 아니다(§5).
2. **청크 수 희석**: docling 673, MinerU 306청크 vs baseline 55청크. top_k=5 고정에서 청크가 잘게 쪼개진 축은 정답 청크가 5위 밖으로 밀린다. 실제로 docling이 놓친 valuation V01–V03(SK하이닉스/삼성전자 PER·PBR)은 **전부 커버리지는 통과(cov=True)** 인데 top-5에 안 든 것 — 파싱 실패가 아니라 검색 희석이다. top_k를 키우면 회복된다(§4.4).

### 4.3 진짜 파싱 실패 — 깊게 병합된 표(삼성 p23)에서 갈린다

`extraction coverage`(청크 무관)로 보면 실패 지점이 선명하다.

| 축 | 커버리지 | 놓친 문항 |
|---|---:|---|
| baseline | **1.00** | 없음 |
| docling | 0.97 | **F04**(DRAM 479.0) |
| enhanced | 0.90 | T10(ON Semi), **F04**(DRAM), C02(도표11 제목) |
| MinerU | 0.90 | **F03**(Memory 505.1), **F04**(DRAM 479.0), **F05**(영업이익률 72%) |

원인은 p23 **삼성전자 사업부문별 실적표**(반도체→Memory→DRAM→NAND 각각 매출/영업이익/이익률 × 11개 시점, rowspan 다중 병합)다.

- **MinerU**: rowspan 병합 셀을 재구성하다 **라벨-값 정렬이 깨졌다** — `영업이익 영업이익률`을 한 셀로 합치고, `Memory`/`DRAM` 세그먼트 라벨이 자기 매출액 행에서 떨어져 나갔다. → 이 표에서 커버리지 3개 실패(F03/F04/F05). **가장 취약.**
- **docling**: 파이프표로 평탄화가 훨씬 정확(반도체→534.9, Memory→505.1 유지). 단 DRAM 행의 라벨 셀이 비어(중첩 서브라벨 누락) F04만 실패.
- **enhanced**: canonical 매칭으로 F01/F02/F03/F05 잡음, F04만 실패(docling과 동일 원인).
- **baseline**: 무식하게 읽기 순서를 보존해 F04 포함 전부 인접 — **깊은 병합표에서는 "구조 재구성"이 오히려 중첩 라벨을 흘리고, 나이브 읽기순서가 이를 보존**하는 역설.

> 정리: **가장 어려운 병합표에서 enhanced ≈ docling(4/5) > MinerU(0/5)**. MinerU는 캡션·단순표는 완벽하나(§5) 깊은 rowspan 표에서 무너진다.

### 4.4 검색 희석 보정 (top_k 민감도) — 공정성 확인

청크 수 차이가 hit@5를 왜곡한다는 §4.2 가설을 top_k=10으로 재측정해 검증했다.

| 축 | 커버리지 | hit@5 | **hit@10** | 유형별 hit@10 |
|---|---:|---:|---:|---|
| baseline | 1.00 | 0.93 | **1.00** | 전 유형 1.00 |
| docling | 0.97 | 0.83 | **0.97** | valuation 0.63→**1.00**, text→1.00, finance 0.80(F04만 실패) |
| enhanced | 0.90 | 0.87 | 0.87 | 변화 없음(놓친 건 검색이 아니라 커버리지 실패) |
| MinerU | 0.90 | 0.80 | 0.83 | valuation→1.00, **finance 0.00 그대로** |

**해석 — 이게 결정적이다**:
- **docling**: 0.83→**0.97** 로 회복. k=5에서 밀린 valuation V01–V03은 **전부 검색 희석**이었음이 확정(파싱은 정상). 희석 보정 후 docling이 구조화 파서 중 검색 최상.
- **MinerU finance는 k=10에서도 0.00** — 희석이 아니라 **진짜 실패**. 삼성 병합표를 잘못 재구성해(§4.3) 값이 라벨과 붙지 못하니 아무리 많이 뽑아도 근접-정답 청크가 없다.
- **enhanced는 k를 키워도 그대로(0.87)** — 놓친 3건(T10/F04/C02)이 검색이 아니라 **커버리지(파싱·정제) 단계 손실**이라는 뜻. → 개선 포인트가 검색이 아니라 §5의 캡션 과삭제·§4.3 병합표에 있음을 정확히 지목.

> **공정 비교 결론**: 청크 희석을 보정하면 **baseline 1.00 ≥ docling 0.97 > enhanced 0.87 ≥ MinerU 0.83**. 구조화 파서 3개 중 **검색 end-to-end는 docling이 최상, MinerU가 병합표 때문에 최하**. enhanced는 그 사이이며 약점이 "검색"이 아니라 "정제 과삭제"로 특정됨.

---

## 5. 출력 구조 (아웃풋 스트럭처를 잘 뽑는가)

검색 점수만 보면 baseline이 좋아 보이지만, **다운스트림(컬럼 정확한 LLM 추론 + Entity Fusion → Supabase 저장)** 관점에서 구조 품질은 완전히 다르다.

| 지표 | baseline | enhanced | docling | MinerU |
|---|---|---|---|---|
| 표를 **기계가독 객체**로 출력 | ✗ 평면 텍스트 | ✓ 447 canonical record | ✓ 31 파이프표 | ✓ 34 HTML표 |
| 컬럼 헤더 ↔ 값 매핑 | ✗ (헤더 흩어짐) | ✓ (canonical_field) | ✓ | ✓(단순표)/✗(깊은병합) |
| 도표 캡션 보존(/93) | 93 | 87 | **93** | **93** |
| 표 캡션 보존(/11) | 11 | 9 | **11** | **11** |
| 섹션 경로(section_path) | ✗ | ✓ 평균깊이 1.37, 40청크 | (헤더만) | (헤더만) |

- **baseline의 함정**: `8.7`을 찾긴 하지만 그게 "26F PER"인지 "27F PER"인지 **컬럼 라벨이 값에 붙어있지 않다**. 헤더행(1W/1M/YTD/26F/27F…)이 원문 위쪽에 따로 흩어져 있어, 값만으로는 어느 컬럼인지 기계가 확정 못 한다. 즉 **검색은 되지만 저장·정형화는 불가**. docling/MinerU/enhanced만 컬럼-값 매핑을 준다.
- **enhanced의 약한 회귀**: 헤더/푸터·보일러플레이트 정제 과정에서 **도표 캡션 6개·표 캡션 2개를 흘렸다**(87/93, 9/11). docling·MinerU는 정제를 안 하니 캡션 100% 보존. → 고도화의 정제 단계가 캡션 일부를 과삭제하는 버그성 손실(개선 여지).
- **enhanced canonical 매칭**: 447행/3252레코드, hit_rate 13.8% — LGCNS(11.9%)와 일관. 낮은 건 설계상(순수 재무항목은 이미 DB에 있어 필터, 34행 필터됨) 당연한 결과이며, 매칭 안 된 나머지는 원문 보존.

---

## 6. 페이지 분류 (페이지 분류를 잘 하는가) — enhanced 전용

YOLOv11n 페이지 분류 결과 vs 정답 페이지타입(31p):

| 라벨 | TP | FP | FN | Precision | Recall | F1 |
|---|---:|---:|---:|---:|---:|---:|
| **table** | 15 | 0 | 0 | **1.00** | **1.00** | **1.00** |
| **image/chart** | 17 | 4 | 0 | 0.81 | **1.00** | 0.90 |

- **표 페이지 분류 완벽**(15/15, 오탐 0). 밸류에이션표(p1–6)·실적표(p23–31) 전부 정확히 table로 잡음.
- **차트 페이지 재현율 100%**(도표 페이지 17개 모두 image로 잡음) — 라우팅 관점에서 가장 중요(놓치면 안 됨). 정밀도 0.81은 **p24/29/30/31 실적표 페이지에서 Picture 박스를 4건 오탐**한 것(작은 로고/표 테두리 오인 추정). 과탐이라 안전측 오류.
- docling/MinerU/baseline은 **페이지 분류 자체가 없다** — 전 페이지를 균일 처리한다. 이게 그들이 느린 이유이자, 페이지별 최적 경로로 못 가는 이유.

---

## 7. 라우팅 (라우팅을 잘 하는가) — enhanced 전용

- **난이도 라우팅**: 31p 중 **p31 1건만 "hard"로 판정 → MinerU로 라우팅**. 정답 기대치는 hard 0건(전부 디지털 텍스트)이므로 **1건 오라우팅(과라우팅)**. p31은 실적표 페이지인데 컬럼 중첩+추출 순서 지그재그가 임계를 넘겨 hard로 오판. 비용 관점 손해는 작지만(1/31), 불필요한 MinerU 호출 1회.
  - 흥미로운 상관: p31은 §6의 **image 오탐 4개 중 하나** — 이 페이지 레이아웃이 실제로 파서에 까다로웠다는 신호가 두 지표에서 겹친다.
- **타입 라우팅**: 텍스트→텍스트 경로, 표→TATR 경로, 차트 페이지→(이미지 모듈, 이번엔 off) 로 정확히 분기. 표 페이지 판정 정밀도 1.0이라 표 라우팅 오분기 0.

---

## 8. 종합 판정

| 관점 | 승자 | 근거 |
|---|---|---|
| **지연시간** | baseline ≫ **enhanced** | enhanced가 라우팅으로 docling 8.4×·MinerU 5.0× 빠름. "only" 방식은 전 페이지 무거운 파싱 |
| **원문 답 추출(이 PDF 한정)** | baseline | 깨끗한 디지털 PDF라 나이브 추출로도 커버리지 1.0. *단 이 문서에 국한* |
| **출력 구조(기계가독)** | **docling ≈ enhanced ≈ MinerU** ≫ baseline | baseline은 컬럼-값 매핑 없음. 저장/정형화 불가 |
| **깊은 병합표 정확도** | **enhanced ≈ docling** > MinerU | MinerU rowspan 재구성이 깨짐(F03/04/05 실패) |
| **캡션 보존** | docling = MinerU(100%) > baseline(100%*) > enhanced(87·9) | enhanced 정제가 캡션 일부 과삭제 |
| **페이지 분류·라우팅** | **enhanced (유일)** | table F1 1.0, chart F1 0.90. 나머지 3축은 기능 없음 |

### 무엇을 택할 것인가

- **고도화 파이프라인의 존재 이유가 이 실험으로 확인됨**: (1) **라우팅으로 속도**(docling/MinerU-only의 1/5~1/8 시간), (2) **페이지 분류·라우팅이라는 고유 기능**(다른 축엔 없음), (3) **구조화 출력(canonical/section_path)** 을 docling·MinerU급으로 뽑으면서 훨씬 빠름. 깨끗한 PDF에서 원문 커버리지가 baseline보다 낮은 건 문제가 아니라 **정제·청킹·구조화의 대가**이며, 진짜 이득은 다운스트림(저장·컬럼정확 추론)에서 나온다.
- **개선 과제 (이 실험이 새로 드러냄)**:
  1. **캡션 과삭제**: 헤더/푸터·보일러플레이트 정제가 도표 캡션 6개를 흘림 → 차트 페이지에서 캡션 라인은 정제 예외 처리 필요.
  2. **p31 오라우팅**: 실적표 페이지가 hard로 오판 → 난이도 판정에 "표 밀집 페이지" 예외 or image-FP 상관 활용.
  3. **깊은 병합표(F04)**: enhanced·docling·MinerU 모두 중첩 서브라벨(DRAM)을 흘림. baseline만 읽기순서로 보존 → TATR/canonical에 rowspan 중첩 라벨 상속 로직 보강.
  4. **MinerU를 "어려운 페이지"에만 쓰는 현 설계가 옳다는 근거**: MinerU-only는 느리고(236s) 깊은 병합표에서 취약(finance 0.0). 통째로 MinerU에 맡길 이유가 없다.

---

## 9. 엔티티·메타데이터 추출 비교 (구조화 출력 = 파이프라인 실제 단계)

§2~8이 파싱/검색이라면, 이 섹션은 **페이지 분류 이후 텍스트/테이블/이미지 라우팅 끝단의 구조화 출력**(ERD의 "Structured output (OpenAPI)" → Entity Fusion) 품질을 4축 비교한다. **각 파서 산출물을 같은 추출기(gpt-4o-mini, `structured_output.extract_text_chunk_metadata`)에 먹여** 어느 파서 출력이 엔티티를 더 잘 뽑히게 하는지 본다.

- **공정성**: 4축 모두 full_text를 **동일한 ~1800자 윈도우**로 잘라 투입(청크 수 차이 confound 제거). 표/차트 텍스트는 full_text에 포함되므로 3브랜치 모두 커버.
- **정답셋**: `ground_truth_entities_smartphone.json` — 육안 검증된 46개 엔티티를 3브랜치로 태깅(텍스트=thesis 9, 테이블=표1+표2 40, 이미지=도표 범례 7). 채점은 팀 `evaluate.py` 관례(별칭 정규화 매칭). Recall이 주지표(정답셋이 검증된 subset이라 precision은 lenient 기준).

### 9.1 결과

| 축 | 추출 지연 | **R 전체** | R 텍스트 | **R 테이블** | R 이미지 | P(lenient) | F1 | 추출 엔티티수 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| **baseline** | 47.5s | **0.33** | 0.67 | **0.375** | 0.43 | 0.98 | 0.49 | 62 |
| enhanced | 54.6s | 0.67 | 0.78 | 0.68 | 0.86 | 0.99 | 0.80 | 109 |
| docling | 99.4s | 0.70 | 0.89 | 0.70 | 0.86 | 0.99 | 0.82 | 156 |
| **MinerU** | 69.7s | **0.72** | 0.89 | **0.73** | 0.86 | 0.99 | 0.83 | 145 |

### 9.2 핵심 발견 — 구조가 엔티티 recall을 ~2배로

- **baseline 0.33 vs 구조화 파서 0.67~0.72 (약 2배 격차).** 검색 테스트(§4)에선 깨끗한 PDF라 네 축이 비슷했지만, **엔티티 추출에서는 구조 유무가 결정적**이다.
- **원인 검증**: baseline이 놓친 표2 종목(Nvidia·AMD·ASML·Broadcom·TSMC·Intel·Marvell...)은 **baseline 원문에 전부 존재한다**(직접 확인). 데이터가 없어서가 아니라, LLM 추출기가 **PyMuPDF의 "숫자 벽"(구조 없는 표 덤프)에서 기업명을 신뢰성 있게 못 뽑는다.** docling/MinerU/enhanced는 같은 종목을 **깔끔한 표 행(`| Nvidia | 4,659,226 | ... |`)** 으로 주니 LLM이 안정적으로 추출.
- **격차가 가장 큰 곳은 테이블 브랜치**: baseline 0.375 → 구조화 0.68~0.73. 텍스트 브랜치 격차는 작고(0.67 vs 0.78~0.89), 이미지(도표 범례) 브랜치도 baseline 0.43 vs 0.86.
- **구조화 파서 3종은 근소차**(MinerU 0.72 ≈ docling 0.70 > enhanced 0.67). MinerU/docling이 표를 그대로 마크다운/HTML로 보존해 표 종목 recall이 약간 높고, enhanced는 canonical 필터·정제로 일부 소형주(브이엠·씨엠티엑스·파크시스템스)를 흘림.
- **추출 엔티티 수**: baseline 62 vs 구조화 145~156 — 구조화 파서가 2~2.5배 더 많이 뽑는다(표3~11의 대만/글로벌 종목까지 표 구조로 노출). Precision(lenient) 0.98~0.99로 오검출은 거의 없음(정답셋 밖 실기업은 out-of-scope로 관대 처리).

### 9.3 메타데이터 (감성·섹터)

| 축 | 감성 분포(neg/neu/pos) | 섹터 감지 |
|---|---|---|
| baseline | 16 / 13 / 6 | 반도체 |
| enhanced | 11 / 24 / 1 | 반도체, 화학, IT |
| docling | 26 / 37 / 6 | 반도체, 반도체 장비 |
| MinerU | 28 / 31 / 2 | 반도체 |

- **감성**: 네 축 모두 **negative/neutral 우세** — "스마트폰 수요 우려·주가 조정 예상"의 약세 논조를 정확히 포착(정답 기대와 일치). 추출기가 같아 메타데이터 톤은 축 간 유사 → **변별력은 메타데이터가 아니라 엔티티 recall(=입력 구조)에서 나옴**을 재확인.
- **섹터**: 전 축 반도체 감지.

### 9.4 지연 (엔티티 추출 단계)

- 추출 지연은 **텍스트 분량(윈도우 수)에 비례**: baseline 35win·47.5s, enhanced 36win·54.6s, MinerU 61win·69.7s, docling 69win·99.4s.
- docling이 가장 느린 건 마크다운이 표 서식으로 가장 장황(123K자)하기 때문. **파싱 지연(§3)에 이 추출 지연이 더해진다** — 총 파이프라인 지연은 docling ≈ 500s, MinerU ≈ 306s, enhanced ≈ 102s, baseline ≈ 56s(엔티티 추출 포함, 표 나이브 제외).

### 9.5 이 섹션 종합

| 관점 | 결과 |
|---|---|
| **엔티티 recall** | 구조화 파서(0.67~0.72) ≫ baseline(0.33). **구조가 엔티티 추출을 2배로.** 검색으로는 안 보이던 구조의 가치가 여기서 드러남 |
| **구조화 3종 우열** | MinerU ≈ docling > enhanced(근소). enhanced는 정제/canonical로 소형주 일부 손실 |
| **메타데이터** | 감성·섹터는 축 무관하게 일관(추출기 동일). 변별력은 엔티티 recall뿐 |
| **지연** | 추출 지연은 텍스트 분량 비례. enhanced가 구조 품질(recall 0.67) 대비 지연(총 102s) 균형 최상 — docling(500s)/MinerU(306s)의 1/3~1/5 |
| **함의** | Entity Fusion → Supabase(기업명·티커 DB) 단계에선 **구조화 파싱이 필수**. baseline은 원문에 있어도 절반 넘게 못 뽑아 DB 구축 부적합. enhanced는 구조 품질을 docling/MinerU급으로 내면서 훨씬 빨라 **파이프라인 채택 근거가 엔티티 관점에서도 확인됨** |

---

## 10. 고도화 개선 — enhanced_v2 (지연 유지하며 docling/MinerU 상회)

§4~9에서 드러난 enhanced의 약점(캡션 과삭제, 표 선형화 붕괴, 낮은 엔티티/커버리지)을 **지연 중립**으로 고쳐 docling/MinerU를 이기는 게 목표. 원인 진단 → 2개 수정 → 재측정.

### 10.1 진단 (근본 원인)

| 약점 | 근본 원인 (진단으로 확인) |
|---|---|
| 표 엔티티 recall 낮음 | 놓친 종목이 **원문엔 전부 존재**. `build_records`가 wide-form 표를 canonical 레코드로 접으며 **행-엔티티(회사)↔값 연결을 잃음**(`raw_label=값, cells=[]`). 표1 청크가 `기업/지수: | 코스피: | 6,677,715:`처럼 셀 낱개로 흩어짐 |
| 캡션 87/93·9/11 | 헤더/푸터·보일러플레이트 정제가 도표 9·10·11·35·36·46, 표1·표10 캡션을 드롭 |

### 10.2 수정 (둘 다 지연 중립)

1. **표 재선형화**: canonical 붕괴 대신 **파이프라인 자체 row parser**(`parse_table_adaptive` TATR-grid / `parse_simple_table_from_words`)의 행 결과 `{label, cells}`를 **행단위 마크다운(`| 삼성전자 | 1,984,812 | … | 7.6 | 5.5 |`)** 으로 직접 사용. 재무행 필터·canonical collapse 제거로 회사-값 보존. TATR은 그대로라 지연 동일.
2. **캡션 보호**: 원문(PyMuPDF)에서 `도표/표 N.` 캡션 라인을 회수해 정제 과삭제 복구 → 93/93·11/11.

### 10.3 개선 전후 (enhanced → enhanced_v2)

| 지표 | enhanced | **enhanced_v2** | 변화 |
|---|---:|---:|---|
| 파싱 지연 | 47.6s | **30.1s** | ↓ 37% (canonical 오버헤드 제거) |
| 도표 캡션 | 87/93 | **93/93** | ✓ |
| 표 캡션 | 9/11 | **11/11** | ✓ |
| 추출 커버리지(prox) | 0.90 | **0.933** | ↑ |
| 검색 hit@5 | 0.867 | **0.90** | ↑ |
| 검색 hit@10 | 0.867 | **0.90** | ↑ |
| 엔티티 R(exhaustive) | 0.957 | 0.957 | = |
| 엔티티 R(sidefield) | 0.674 | 0.674 | = (추출기 병목, §10.5) |

### 10.4 head-to-head vs docling / MinerU

| 지표 | **enhanced_v2** | docling | MinerU | 판정 |
|---|---:|---:|---:|---|
| **파싱 지연** | **30.1s** | 401.6s | 236.5s | **enhanced_v2 압승(13×/8×)** |
| **검색 hit@5** | **0.90** | 0.83 | 0.80 | **enhanced_v2 최고** |
| 검색 hit@10 | 0.90 | **0.967** | 0.83 | docling; ev2 > MinerU |
| 추출 커버리지(prox) | 0.933 | **0.967** | 0.90 | docling 근소; ev2 > MinerU |
| 캡션 보존 | **100%** | 100% | 100% | 동률 |
| 엔티티 R(exhaustive) | 0.957 | 0.957 | **0.978** | 실질 동률 |
| 엔티티 P(lenient) | **0.991** | 0.980 | 0.969 | **enhanced_v2 최고** |
| 페이지분류·라우팅 | **있음(table F1 1.0)** | 없음 | 없음 | **enhanced_v2 전용** |

**결론: enhanced_v2는 지연을 오히려 줄이면서(30s) docling/MinerU를 검색 hit@5·엔티티 정밀도·페이지분류에서 이기고, 캡션·엔티티 recall은 동률, 커버리지·hit@10만 docling에 1문항 차로 근소 열세.** 실무적으로 "같거나 더 나은 품질을 8~13배 빠르게" — 명확한 우위.

### 10.5 남은 갭 (정직하게)

- **커버리지 1문항 차(docling 0.967 vs 0.933)**: ① **T10** — 본문(thesis)의 "ON Semiconductor" 엔티티가 enhanced 텍스트 정제에서 유실(표엔 있으나 본문 언급 소실). ② **F04** — 삼성 p23 **깊은 rowspan 병합표**의 DRAM 매출 479.0을 row parser가 놓침(docling도 coverage에선 이 표를 겨우 잡은 최난 케이스). 둘 다 단일 문항·최난 케이스.
- **엔티티 추출기 병목**: sidefield 추출기(`extract_text_chunk_metadata`)로는 구조를 고쳐도 recall 0.674로 정체 — **장꼬리 표 종목을 덜 나열**하기 때문. **exhaustive 프롬프트**(팀 `extract_entities_and_eval` 방식)로 바꾸면 전 축이 0.96~1.0로 포화 → **구조는 약한 추출기에서 2배 효과, 강한 추출기에선 거의 무의미**(중요 통찰). 즉 enhanced_v2는 강추출기에선 docling/MinerU와 동률, 약추출기에선 여전히 baseline의 2배.
- **p31 오라우팅** 미해결(§7) — 하단 컴플라이언스 2단 보일러플레이트가 원인. 별도 수정 대기.

### 10.6 개선 요지 재현

```
work/axis_enhanced_v2.py        표 row-parser 재선형화 + 캡션 회수 (parse 30.1s)
work/entity_extract_exhaustive.py  exhaustive 엔티티 패스(4축 공정) → 포화 검증
out_enhanced_v2.json / results.json / results_entities.json  (enhanced_v2 포함)
```

---

## 11. A/B 테스트 — enhanced_v2(B) vs 4개 모델(A)

같은 30문항/46엔티티를 두 모델에 돌린 **대응표본**이므로, 문항단위 win/loss/tie + **McNemar 정확 이항검정**(불일치쌍 부호검정)으로 유의성을 판정했다. 하네스 `work/ab_test.py` → `results_abtest.json`.

### 11.1 매치업별 결과 (B_win / A_win / net / p)

| 대결 | 검색 hit@5 | 검색 hit@10 | 커버리지 | 엔티티(기본추출기) | 엔티티(exhaustive) | 지연 |
|---|---|---|---|---|---|---|
| **vs baseline** | 1/2 (−1) | 0/3 (−3) | 0/2 (−2) | **18/2 (+16) p=.0004\*** | 0/2 (−2) | baseline 0.16s(구조·분류 없음) |
| **vs enhanced** | 1/0 (+1) | 1/0 (+1) | 1/0 (+1) | 7/7 (0) | 0/0 (0) | **B 1.6× 빠름** |
| **vs docling** | **4/2 (+2)** | 0/2 (−2) | 0/1 (−1) | 4/5 (−1) | 0/0 (0) | **B 13.3× 빠름** |
| **vs mineru** | **5/2 (+3)** | **4/2 (+2)** | 2/1 (+1) | 4/6 (−2) | 0/1 (−1) | **B 7.9× 빠름** |

`*` = p<0.05 (유의). net = B_win − A_win.

### 11.2 통계적으로 유의한 차이는 단 하나

**전체 A/B에서 p<0.05인 지표는 「enhanced_v2 vs baseline · 엔티티(파이프라인 기본 추출기)」 단 하나** — B_win 18 / A_win 2, **p=0.0004**. 즉 **파이프라인의 실제 추출기로 엔티티를 뽑을 때, 구조화 파서(enhanced_v2)가 baseline을 압도적·유의하게 이긴다.** 이것이 이 실험의 가장 단단한 결론(Entity Fusion→DB 단계에서 구조가 결정적).

나머지 검색·커버리지 차이는 **전부 유의하지 않다(p≥0.25)**. 깨끗한 디지털 PDF + 30문항 표본에서 네 모델의 "답 검색 가능성"은 통계적으로 동등하다는 뜻 — 어느 파서도 서로를 유의하게 못 이긴다.

### 11.3 판정

| 대결 | 품질 | 지연 | 종합 |
|---|---|---|---|
| vs **enhanced** | 3승 0패 1무(유의X, 일관되게 우세) | 1.6× 빠름 | **enhanced_v2 명확 우위**(개선 성공) |
| vs **MinerU** | 검색/커버리지 우세(hit@5 +3), 유의X | 7.9× 빠름 | **enhanced_v2 실무 우위**(동등품질·8배속) |
| vs **docling** | 혼전(hit@5 이기고 hit@10/cov 근소 짐), 유의X | 13.3× 빠름 | **enhanced_v2 실무 우위**(동등품질·13배속) |
| vs **baseline** | 검색은 baseline이 근소 우위(유의X)·**엔티티는 B가 유의하게 압승** | baseline이 파싱만 빠름 | **enhanced_v2 우위**(구조·분류·엔티티에서 baseline은 DB 부적합) |

**결론**:
1. **유의한 우위는 "구조 vs 무구조 엔티티 추출"(B≫baseline, p=.0004)뿐** — 이 문서에서 검색 답찾기는 네 모델 통계적 동등.
2. 따라서 **실무 결정 변수는 지연** — enhanced_v2는 docling/MinerU와 **동등 품질을 8~13× 빠르게**, 개선 전 enhanced보다 **더 빠르고 전 지표 우세**.
3. baseline은 이 깨끗한 PDF의 검색에선 값싸고 경쟁력 있으나, **구조화 출력·페이지분류·엔티티(DB)에서 탈락** → 파이프라인 부적합.
4. 표본 30문항이라 검정력이 낮음(대부분 p>0.25) — 더 강한 결론은 여러 PDF·문항 확대가 필요하다는 점을 정직하게 명시.

---

## 부록. 재현 방법

```
pdf_pipeline/final/
├── ground_truth_smartphone.json          검색 정답셋 30문항 + 페이지타입
├── ground_truth_entities_smartphone.json 엔티티 정답셋 46개(3브랜치) + 별칭
├── results.json / results_k10.json       검색 교차평가(top_k 5 / 10)
├── results_entities.json                 엔티티 추출 교차평가
└── work/
    ├── common_exp.py                     청킹 + 근접성 채점 + BGE/BM25 하이브리드
    ├── axis_baseline.py  axis_enhanced.py  axis_docling.py  axis_mineru.py
    ├── out_{baseline,enhanced,docling,mineru}.json   각 축 파싱 산출물+지표
    ├── evaluate_axes.py                   4축 검색 교차평가 → results.json
    ├── entity_extract.py                  각 축 → gpt-4o-mini 엔티티/메타 추출(윈도우 통일)
    ├── entity_out_{axis}.json             각 축 추출 엔티티+메타+지연
    ├── entity_score.py                    브랜치별 recall + P/R/F1 → results_entities.json
    └── diag_p31.py / diag_p31_clusters.py p31 오라우팅 원인 진단
```

- venv: `pdfex/demo_venv`(mineru+openai+pymupdf+torch-cpu)에 docling·pdfplumber·rank-bm25·ultralytics·sentence-transformers·timm 추가.
- YOLO 가중치: `Armaggheddon/yolo11-document-layout`(HF 캐시) → `pdf_pipeline/page_classification/models/yolo11n_doc_layout.pt`.
- 임베딩: `dragonkue/BGE-m3-ko`(4축 공통 고정). 검색: BM25+dense 가중합(0.7/0.3).
- 실행: `python axis_*.py` → `EVAL_TOPK=5 python evaluate_axes.py`.
- **주의**: OpenAI/Supabase 키는 `.env`(gitignore)에만 저장. 로그·산출물·본 문서에 키 미포함.

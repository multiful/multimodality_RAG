# 파이프라인 최종 정리 — 팀원 인계용

작성 기준: `pdf_pipeline/reference/Construct/20260721_industry_362851000.pdf`(하나증권 건설업 Weekly, 10p, 다기업 산업 섹터 리포트)를 실제로 엔드투엔드 실행한 결과. 쿼리: **"여기 나온 기업의 인사이트 도출해주고, 건설업종 종목 주간 수익률이 가장 높은 기업은 어딘지 추출해"**.

재현 방법:
```python
from run_investment_opinion_demo import main
result = main(
    pdf_path="pdf_pipeline/reference/Construct/20260721_industry_362851000.pdf",
    pdf_id="Construct", ticker=None,   # 산업 섹터 리포트라 단일 티커 없음
    query="여기 나온 기업의 인사이트 도출해주고, 건설업종 종목 주간 수익률이 가장 높은 기업은 어딘지 추출해",
    add_structured_metadata=True, sector="건설",
)
```
(2026-07-24 기준, `main()`이 인자를 받도록 파라미터화됨 — 인자 없이 부르면 기존 LGCNS 기본값)

전체 소요시간 **18.0s**(YOLO/분류 0.8s + 3브랜치 동시실행 10.8s + 검색 0.08s + 생성 6.3s), evidence 238건(text 16 + table 206 + image 16) 전량 Supabase `document_evidence`(pdf_id=`Construct`, ticker=`NULL`) 적재 확인.

---

## 0. 페이지 분류 기법 + 페이지 난이도 분류 기법

**모델/기법**:
- **레이아웃 탐지**: `YOLOv11n`(ultralytics, 커스텀 학습 `yolo11n_doc_layout.pt`) — 페이지를 150dpi로 렌더링해 텍스트/표/이미지 bbox를 한 번에 탐지. `pdf_pipeline/yolo_layout.py`의 `run_yolo_layout()`이 페이지당 정확히 **1회**만 호출되고, 그 결과(`page_boxes`)를 page_classification/text_processing/table_processing 세 곳이 전부 공유·재사용(중복 추론 제거, [8]/[10]/[26] 최적화 이미 적용됨).
- **1차 신속검사**: `PyMuPDF`(fitz) 메타데이터로 쉬운/어려운 PDF를 먼저 가른다(규칙 기반, 모델 없음).
- **난이도(easy/hard) 판정**: `pdf_pipeline/text_processing/reading_order_router.py`의 `assess_page_difficulty()` — 순수 규칙 기반. 컬럼이 기하학적으로 겹쳐도 원문 추출 순서가 컬럼별로 잘 그룹돼 있으면 문제 없다는 관찰을 반영해 `material_overlaps AND interleaving_excess>=2`를 **동시에** 만족할 때만 hard로 판정(Claude가 실제 문서 전수 검토해 캘리브레이션).
- **스캔본 감지**: `scanned_page_router.py`의 `detect_scanned_pages()` — 텍스트 레이어 유무 + 이미지가 페이지를 덮는 비율, 순수 PyMuPDF(모델 없음). hard 판정과는 독립적인 별개 신호.
- hard 페이지는 `MinerU`(PDF-Extract-Kit-1.0, pipeline 백엔드)로 라우팅.

**Construct 실행 결과**: 10페이지 분류 0.76s(YOLO 로드 포함), 스캔본 0개, hard 페이지 0개(전 페이지 easy — 우리 파이프라인이 끝까지 처리).

---

## 1. 이미지 최종 파이프라인 + 구조화 출력 구조

**경로**: `pdf_pipeline/image_processing/` (s1_parse.py → s2_onestop_mineru.py). 오늘 `~/minerU`(별도 Python 3.11 venv)로 **실제 실행**해 검증(이전엔 이 환경에 MinerU가 없어 대체 카드로만 검증했었음 — 이번에 실제 MinerU 연결까지 마침).

**모델 스택(단계별)**:
| 단계 | 모델 | 비고 |
|---|---|---|
| [0] 레이아웃+OCR | `MinerU`(PDF-Extract-Kit-1.0, pipeline 백엔드) | chart/image/table bbox + 캡션/각주 OCR |
| [1] 규칙필터 | 없음(크기/종횡비 규칙) | chart는 크기 무관 통과 |
| [2] 크롭 OCR | `PP-OCRv6`(det) + `PP-OCRv5`(rec, korean) | MinerU 내장, ~0.4~0.5s/장 |
| [3] 그림 분류기 | `DocumentFigureClassifier-v2.5`(EfficientNet-B0, 4M) | 26종 세부라벨, MinerU 탐지와 상호검증만(판정 자체는 안 바꿈) |
| [4a] 차트→표 (옵션) | `MinerU2.5-Pro-2605-1.2B` | 15.5s/장, 이번 실행에선 미사용(OCR만으로 충분해 생략, 근거는 §4) |
| [4b] 서술형 해석 (옵션) | `qwen3:8b`(Ollama, 로컬) | 4a의 표를 문체 변환, 3.5s/장 |
| [5] 구조화 출력 (옵션) | `gpt-4o-mini` | 카드별 정성 메타데이터 |

**Construct 실행 결과**: MinerU 파싱 34.0s(10p) → OCR/분류 12.0s → 총 32블록(chart/image 16 useful, table 16건은 판정 없이 표 브랜치로 인계`status=handoff`). `--with-chart-analysis` 없이도 PP-OCR만으로 "도표 3. 건설업종 종목 주간 수익률" 차트에서 회사명+수치가 정확히 읽혔음(§4 참고) — 이번 문서는 4a/4b 없이도 충분했다.

**구조화 출력 스키마**(`structured_output.ImageMetadata`, `gpt-4o-mini`): 실제 예시(도표 1. KOSPI 및 건설섹터 수익률 카드) —
```json
{
  "image_type": "chart",
  "caption_or_title": "도표 1. KOSPI 및 건설섹터 수익률",
  "entities_mentioned": ["Quantiwise", "하나증권"],
  "described_content": "이 차트는 KOSPI와 건설섹터의 수익률 변화를 보여준다.",
  "key_values_or_trend": "KOSPI가 400, 건설업이 360 수준에서 변화하고 있다.",
  "time_period": "25.1부터 26.7까지"
}
```
안정 필드(`onestop_cards.jsonl`): `image_id, doc_id, page, block_type, bbox, det_score, caption, footnote, status, filter_stage, embed_text, crop` — 항상 채워짐. `ocr{text,n_boxes,mean_conf}`/`clf_label`/`chart_table`/`narrative`는 옵션 단계를 켰을 때만.

**⚠ 이번에 발견·수정한 이슈 3건**:
1. **경로 불일치(버그)**: `image_processing/common.py`의 `PROJECT_ROOT`가 리포 루트가 아니라 `pdf_pipeline/` 자신이라(`Path(__file__).resolve().parent.parent`), 실제 산출물은 `pdf_pipeline/data/onestop/{doc_id}/`에 쓰이는데 데모(`run_investment_opinion_demo.py`)는 리포 루트 기준 `data/onestop/`을 보고 있어서 **실제 MinerU를 돌려도 항상 폴백 카드만 쓰던** 상태였음. `ONESTOP_CARDS_PATH`를 `pdf_pipeline/data/onestop/`로 수정.
2. **부호 오독(생성 단계 버그, §6에서 상술)**: 차트 OCR 텍스트의 "값(N)" 괄호 표기(한국 증권 리포트 관례상 음수/하락)를 LLM이 잘못 읽는 문제 — 데이터 정규화로 해결.
3. **캐싱 미배선**: `common.py`에 content-hash 기반 로컬 디스크 캐시(`cache_get`/`cache_put`, `data/cache/{stage}/{hash}.json`)가 이미 구현돼 있었는데 `ocr_crop`/`chart_table_extract`/`narrative_from_table` 어디서도 호출을 안 해서 죽은 코드였음 — 세 함수 모두에 배선. **실측**(LGCNS, `--force`로 카드 단위 resume을 우회하고 content-hash 캐시만으로 검증): 1차 실행 7.4s(캐시 생성, "MinerU OCR 로드 완료" 로그 있음) → 2차 실행 **4.1s**(OCR 엔진 로드 로그 자체가 사라짐 — 6개 크롭 전부 캐시 히트). Redis 아님, 로컬 파일 캐시라 별도 인프라/키 불필요. 실패(None) 결과는 캐시하지 않음(일시 오류 영구화 방지).

---

## 2. 텍스트 최종 파이프라인 + 구조화 출력 구조

**경로**: `pdf_pipeline/text_processing/text_extraction.py` (`process_pdf`/`process_pdf_streaming`).

**모델 스택**:
| 단계 | 기법/모델 |
|---|---|
| 추출 | `PyMuPDF`(fitz) `get_text()` — whole_page 방식, recall 100%/9.5ms(베이스라인 검증됨) |
| 정제 | PUA 글리프 제거 + 헤더/푸터 제거(좌표+반복빈도) + 구두점/기호 정규화(전부 규칙 기반) |
| 청킹 | Rule-based 계층적 청킹(`contextual_chunker.chunk_contextual_production`) — section_path 부여 |
| boilerplate 제거 | Sentence-Embedding 유사도(`dragonkue/BGE-m3-ko`) 기반, 문서 뒤쪽 구간만 게이팅해 비용 절감 |
| 구조화 출력 | `gpt-4o-mini`(OpenAI Structured Output, Pydantic) |

**구조화 출력 스키마**(`structured_output.TextChunkMetadata`): 실제 예시(page1, 첫 청크) —
```json
{
  "chunk_index": 0, "entities": [], "sector_mentioned": null,
  "topic": "실적발표 시작", "metric_mentions": [], "time_period": "2026.7.21",
  "sentiment": "neutral", "contains_forward_looking_statement": false
}
```
(청크 8개 초과 시 `ThreadPoolExecutor`로 배치 병렬 호출, [47] 실측 12.95s→8.22s)

**Construct 실행 결과**: 16개 청크 생성, 전량 Supabase 적재(브랜치 개별 10.77s, 구조화 출력 포함 — off 상태면 훨씬 빠름).

---

## 3. 테이블 최종 파이프라인 + 구조화 출력 구조

**경로**: `pdf_pipeline/table_processing/` (`adaptive_table_router.py` → `row_parser.py` → `run_table_metadata_pipeline.py`).

**⚠ 오늘 가장 크게 바뀐 부분** — 표 파싱 알고리즘 자체를 교체(재일님이 `pdf_pipeline/final/`에서 15문서 A/B로 검증해둔 결과를 프로덕션에 처음 연결):

| 단계 | 기법/모델 |
|---|---|
| 표 위치 탐지 | `YOLOv11n`(공유, 재추론 없음) |
| **1차 시도** | `pdfplumber` text-strategy(선/괘선이 아니라 글자 좌표 기반) + `_text_strategy_gate()`(열수 일관성 or 숫자비율로 신뢰도 자체 판정) |
| **2차 폴백** | word-clustering(`parse_simple_table_from_words`, 좌표 기하학 기반) |
| **3차 안전망(오늘 추가)** | `TATR`(microsoft/table-transformer-structure-recognition) — complexity=complex이고 1·2차 결과가 "값이 여러 개 한 셀에 뭉쳐있어" 보이는 표에서만, 프로세스당 1회 지연 로드, **CPU 고정**(사유는 §5 하단 "동시성 버그" 참고) |
| 정규화 | 한글 과잉 띄어쓰기 보정, 값 타입별 정제, cid 글리프 오류 탐지 |
| 매칭 | `canonical_field_schema.py`(alias 사전 기반 표준 재무필드 매핑) |
| 구조화 출력 | `gpt-4o-mini`(표 단위 정성 메타데이터) |

**표 파서 계보(재일님 실험, `pdf_pipeline/final/실험_4축_비교_스마트폰.md`, 15문서·387개 consensus 엔티티 pooled)**:

| 버전 | 핵심 | 엔티티 recall | 파싱 속도(문서당) |
|---|---:|---:|---:|
| baseline(무구조) | pdfplumber만 | 0.672 | 0.07s |
| docling | Docling TableFormer | 0.788 | 81.3s |
| MinerU | MinerU 표 파서 | 0.786 | 97.5s |
| **enhanced_v5(채택)** | **하이브리드 게이트** | **0.835(최고)** | **8.5s** |

McNemar 검정: v5 vs baseline **p<0.0001**(유의 압도), v5 vs docling/MinerU **p≈0.10**(근소 우세, 완전 유의는 아님 — 정직하게 명시).

**구조화 출력 스키마**(`structured_output.TableMetadata`): 실제 예시(page4 표) —
```json
{
  "table_title": "수익 비율 변화", "entities_mentioned": [],
  "time_periods_covered": [], "table_type_refined": "실적 요약",
  "unmapped_fields_summary": "수익과 관련된 여러 지표들", "notable_finding": null
}
```

**Construct 실행 결과**: 표 15개(SIMPLE 1/COMPLEX 14), 223행 파싱 → canonical 매칭 8개(이 리포트는 "재무제표"보다 "투자의견/목표주가/수급" 표 위주라 canonical hit rate이 낮음 — 정상, LGCNS류 재무제표 위주 문서와는 표 성격이 다름), 15건 table_metadata 구조화 출력, 브랜치 개별 8.98s.

---

## 4. 엔티티 추출 예시 + 벡터 DB에서 티커/관련 내용 조회 검증

**Supabase `document_evidence` 실제 스키마**(직접 쿼리 확인):
```
id text PK, pdf_id text, ticker text(nullable), source_type text,
page int, content text, weight float, metadata jsonb, embedding vector, created_at timestamptz
```

**Construct(다기업 산업 리포트, ticker=NULL로 정상 적재됨)** — `select pdf_id, ticker, source_type, count(*), avg(weight) group by ...`:
```
('Construct', None, 'image', 16, 1.15)
('Construct', None, 'table', 206, 0.60)
('Construct', None, 'text', 16, 1.0)
```
소스별 가중치([48] entity_fusion 설계대로 정상): table(canonical 매칭)=1.3/미매칭=0.6 평균 0.60, image=1.15, text=1.0.

**단일기업(LGCNS) 대조군** — `ticker='064400.KS'`로 정상 태깅:
```
('LGCNS', '064400.KS', 'image', 5, 1.15)
('LGCNS', '064400.KS', 'table', 240, 0.66)
('LGCNS', '064400.KS', 'text', 17, 1.0)
```
→ **단일기업/다기업 문서 모두 ticker 컬럼이 올바르게(NULL 또는 실제 티커) 태깅되고 pdf_id로 문서 단위 조회가 가능함을 확인.**

**쿼리 → 실제 검색된 evidence 예시** (하이브리드 검색 top-8 중 상위 3개, 실측):
| 순위 | 소스 | 페이지 | score | 내용 미리보기 |
|---|---|---|---:|---|
| 1 | text | 2 | 0.947 | "...도표 3. 건설업종 종목 주간 수익률 / 도표 4. 건자재업종 종목 주간 수익률 자료: Quantiwise, 하나증권" |
| 2 | image | 2 | 0.940 | "도표 3. 건설업종 종목 주간 수익률\n...삼성E&A -7.4 현대건설 -7.6 GS건설 -14.3 대우건설 -13.2 ... IPARK현대산업개발 1.3 ..." (정규화 후) |
| 3 | image | 2 | 0.799 | "도표 4. 건자재업종 종목 주간 수익률\n...KCC글라스 2.5 동화기업 -8.1 한샘 11.7 ..." |

→ **쿼리 의도(건설업종 종목 주간 수익률)와 정확히 일치하는 evidence가 최상위로 검색됨.** text/image 두 소스가 같은 도표를 다른 각도(캡션 vs 차트 OCR)로 보완.

### "기업명 및 티커" DB 검색(ERD 하단 노드) — 연결 여부 점검

원본 ERD(`pdf_pipeline/reference/ERD.png`)를 보면 Entity Fusion → DB(supabase) 아래에 "기업명 및 티커"(재무제표 핵심지표 요약을 옆에 붙여서) 노드가 있고, 별도로 임시DB→Hybrid Search→Rank Fusion 결과도 같은 노드로 들어온다 — 즉 **두 개의 Supabase 조회(증거 검색 + 기업 식별)를 병렬로 하고 합치는 설계**다. 점검 결과:

- **실제 연결된 곳은 `streamlit/main.py`뿐이다.** `answer_question(ticker, query)`가 `financial_chunks`/`company_profile_chunks`(dense, `text-embedding-3-small`, 199개 KOSPI200 종목 데이터 있음, 실측 확인)와 `document_evidence`(hybrid, BGE-m3-ko) **세 테이블을 순차로**(병렬 아님) 조회해 합친다. 다만 여기서도 `ticker`는 **검색으로 알아내는 게 아니라 UI에서 사용자가 미리 골라서 주는 값**이다(홈 화면 종목 카드 클릭, 또는 업로드 시 드롭다운 선택) — "PDF 안 엔티티로 DB를 검색해서 티커를 자동으로 찾아낸다"는 흐름은 **어디에도 없다.**
- `run_investment_opinion_demo.py`(이 문서가 검증해온 pdf_pipeline 데모)는 `company_profile_chunks`/`financial_chunks`를 아예 안 건드린다 — `ticker`가 함수 인자로 고정 전달될 뿐.
- **사용자 지시대로 "dense로 추출"을 실제 테스트**: PDF에서 뽑힌 실제 엔티티명(GS건설/현대건설/IPARK현대산업개발/삼성물산)으로 `company_profile_chunks`(dense, GPTEmbedder)를 직접 질의해봤다:

| 질의(한글 기업명) | 정답 티커(yfinance 확인) | dense 검색 top-2 | 정답 히트? |
|---|---|---|---|
| GS건설 | 006360.KS | 078930.KS, 267270.KS | ❌ |
| 현대건설 | 000720.KS | 267270.KS, **000720.KS** | ✅(2위) |
| IPARK현대산업개발 | 294870.KS | 024110.KS, 000720.KS | ❌ |
| 삼성물산 | 028260.KS | 093370.KS, 062040.KS | ❌ |

**4건 중 1건만 부분적으로 맞음 — 이대로 자동 연결하면 안 됨.** 원인: `company_profile_chunks`는 한글이 아니라 **영문 회사명/사업설명**(yfinance longName 등)만 담고 있어서, 한글 기업명 질의와 임베딩 공간에서 제대로 안 붙는다. 이건 애초에 임베딩(의미 유사도)으로 풀 문제가 아니라 **정확한 이름→티커 조회(lookup) 문제** — dense search를 쓰는 게 도구 선택 자체가 안 맞다(프로젝트가 이미 여러 번 확인한 패턴: "이게 진짜 ML/임베딩이 필요한 문제인가"). `data_collection/kospi200_tickers.py`가 pykrx로 `{한글명, 티커}` 쌍을 가져올 수 있지만 이 환경엔 `pykrx` 미설치 + KRX 로그인 계정 필요라 지금 당장 못 씀.

**권장(구현은 보류, 사용자 확인 후 진행)**: 한글명→티커는 (a) `KOSPI200_output`에 이미 있는 199개 프로필을 영文名 그대로 놔두고 한글 별칭 컬럼만 추가하거나, (b) `pykrx`(KRX 계정 필요) 또는 (c) 이미 PDF 구조화 출력이 뽑아준 entities를 그대로 evidence에 태그로 남기고(지금도 됨, §1~3의 structured_metadata.entities) 별도 매칭 없이 LLM이 컨텍스트 안에서 처리하게 두는 현재 방식을 유지 — 셋 중 하나 확정 필요.

---

## 5. 텍스트 인덱싱 과정과 검색 파이프라인 요약

**경로**: `pdf_pipeline/text_processing/index_text.py` + `pdf_pipeline/entity_fusion.py`.

- **인덱싱**: `dragonkue/BGE-m3-ko`(dense, 1024차원, `SentenceTransformer`) + `BM25Okapi`(sparse, 한글 어절 토크나이저). 3브랜치가 각자 계산한 임베딩을 재사용해 통합 인덱스를 만들어 **재임베딩 비용 없음**.
- **Rank Fusion**: `weighted_sum`(min-max 정규화 후 가중합, dense 0.7/bm25 0.3) 또는 `rrf`(Reciprocal Rank Fusion, k=60) — 실측(15개 factoid 질의) 하이브리드가 dense-only 대비 recall@1 **86.7%→93.3%**.
- **쿼리 분류 라우팅**(`route_search`): `gpt-4o` 기반 이진 분류(keyword_specific / abstract) — 25개 라벨셋 실측 규칙기반 80% vs gpt-4o-mini 84% vs **gpt-4o 92%**(채택). **키워드형(keyword_specific)은 `hybrid_search(fusion="rrf")`(dense+BM25)로, 추상형(abstract)은 BM25를 뺀 dense 전용 경로(HyDE 또는 MQE, `use_bm25=False`)로** 갈라진다 — abstract 질의에서 BM25가 순수 노이즈이거나 오히려 해가 된다는 실측([43])이 이 분기의 근거. abstract는 다시 문서 엔티티 수로 한 번 더 갈라 단일기업(entity_count<=1)→HyDE(`gpt-4o-mini`), 다기업(>1)→MQE(`gpt-4o-mini`, 하위질의 4개 생성 후 RRF 융합).
- **entity_count 사전계산**(`precompute_entity_count`): 인제스트 시점에 1회만 계산해 캐시 — 하나증권 포맷이면 정규식(LLM 호출 0회), 아니면 gpt-4o-mini 폴백.
- **Supabase 재조회 경로 확인**: `entity_fusion.load_evidence_from_db(DB_URL, pdf_id="Construct")`로 별도 프로세스에서 DB만 읽어 재인덱싱 → 검색 재실행까지 정상 동작 확인(재수집/재파싱 없이 "저장된 문서에 다시 질의" 가능).

**⚠ 캐싱 추가**: `load_evidence_from_db()`가 매 호출마다 DB 재조회+임베딩 문자열 파싱+BM25 전체 재구축을 하던 문제(사용자 지적) — `(pdf_id, ticker)` 키의 **프로세스 메모리 캐시**(TTL 60초, `invalidate_evidence_cache()`로 수동 무효화 가능)를 추가. **실측**(Construct): 1차 1.0s → 2차(캐시 히트) **0.0000s** → `invalidate_evidence_cache(pdf_id="Construct")` 후 3차 0.74s(정상 재조회 확인). Redis 불필요 — TextIndex가 numpy 배열+BM25Okapi 객체를 들고 있어 Redis에 넣으려면 pickle 직렬화가 추가로 필요한데, 지금 배포가 단일 프로세스라(README: 배포 미구현) 그 복잡도가 아직 정당화 안 됨. [49] RQ 백그라운드 잡처럼 나중에 upsert가 들어오는 경로는 TTL 60초 안에서만 살짝 stale할 수 있음 — 알고 있는 호출측은 즉시 무효화 가능.

### 쿼리 라우팅 재검증 — 4개 문서(핀터멘털/납기/LGCNS/KWave)로 확대 실측

기존 골든셋(`golden_set_event_insight.json`, KWave/LGCNS/납기)에 **핀터멘털**(entity_count=0, 개별종목 투자의견이 아니라 제약/바이오 섹터 트렌드형 리포트라 기존 3개와 다른 유형)을 추가해 `evaluate_retrieval_ab.py`를 4개 문서로 재실행(쿼리: "이 PDF에 나오는 기업의 이벤트 요약과 투자 인사이트를 도출해줘", summary/abstract형).

| 문서 | entity_count | route_search 선택 | 실측 더 나은 쪽 | 일치 |
|---|---:|---|---|---|
| KWave | 6 | MQE | MQE(ndcg 0.889 vs HyDE 0.849) | ✅ |
| LGCNS | 1 | HyDE | HyDE(ndcg 0.967 vs MQE 0.734) | ✅ |
| 납기 | 2 | MQE | MQE(ndcg 0.871 vs HyDE 0.505) | ✅ |
| 핀터멘털 | 0 | HyDE | HyDE(ndcg 0.784 vs MQE 0.626) | ✅ |

**4/4 전부 일치** — entity_count 기반 HyDE/MQE 라우팅이 더 넓은 표본(기존 2~3개→4개, entity_count 0/1/2/6 스펙트럼)에서도 재확인됨.

4개 문서 macro-average(동일 가중):

| 방법 | ndcg | mrr | 지연 |
|---|---:|---:|---:|
| dense-only | 0.554 | 0.317 | 0.05s |
| dense+BM25(hybrid) | 0.428~0.470 | 0.229~0.292 | 0.01~0.02s |
| 라우팅(구, 항상 MQE) | 0.754 | 0.625 | 1.21s |
| **라우팅(신, 엔티티인식)** | **0.848(최고)** | **0.875(최고)** | 2.60s |

abstract형 질의에서 순수 hybrid(dense+BM25)가 dense-only보다 평균적으로 더 나쁨(0.428~0.470 vs 0.554, KWave는 ndcg 0.000까지 하락)도 재확인 — `route_search()`가 abstract 질의엔 hybrid를 안 쓰고 HyDE/MQE(BM25 제외)로 우회하는 설계가 맞다는 근거.

**✅ [수정 완료] route_search를 데모에 배선함**: `run_investment_opinion_demo.py`의 검색 단계(구 7번)가 `entity_fusion.weighted_hybrid_search()`(항상 고정 hybrid) 대신 `index_text.route_search()`(4문서 평균 최고 성능, ndcg 0.848)를 쓰도록 교체 완료. 인제스트 직후 `precompute_entity_count()`도 추가([46] 권장대로 질의 지연에 안 노출되게).

**🔴 그런데 교체 직후 실측하니 이 Construct 쿼리에서 route_search가 오히려 오답을 냄 — 정직하게 기록**: 같은 쿼리로 재실행하니 qtype=`abstract`, entity_count=7~8(구조화 출력 유무에 따라 근소 차이)로 분류돼 MQE(BM25 제외) 경로를 탔고, 최종 답이 **"주간 수익률이 가장 높은 기업은 한샘(11.7%)"**으로 나왔다 — **틀렸다**. 한샘은 도표4(건자재업종) 소속이고, 질의는 명시적으로 "건설업종"을 물었다(정답은 도표3의 IPARK현대산업개발 +1.3%, §6 이전 실측 참고). 근거 로그로 원인 확인:
```
도표4(건자재업종) image score=0.0706  ← 1위
도표3(건설업종)   image score=0.0640  ← 5위, 근소하지만 역전당함
```
`weighted_hybrid_search()`(dense+BM25)로는 도표3이 0.940으로 압도적 1위(BM25가 캡션 "건설업종"/"주간 수익률" 문자 그대로를 정확 매칭)였는데, `route_search()`의 abstract 경로는 `use_bm25=False`로 BM25를 아예 뺀다([43] "abstract 질의엔 BM25가 노이즈"라는 검증된 설계) — 이 쿼리는 그러나 순수 abstract가 아니라 **"인사이트 도출"(진짜 abstract) + "건설업종 종목 중 최고 수익률 추출"(키워드/비교형)이 합쳐진 복합 질의**였고, 뒤쪽 절의 정밀 키워드 매칭 필요성을 분류기(rule-based·LLM 둘 다 "인사이트"/"도출" 트리거 단어 때문에 abstract로만 분류함, 복합 여부는 못 봄)가 못 잡아서 BM25를 통째로 잃은 게 원인.

→ **이게 바로 지난 턴에 논의한 "문맥별 분할 라우팅"이 실제로 필요하다는 최초의 구체적 실패 사례다.** 이전엔 "이론적으론 맞지만 이번 케이스는 단일 hybrid pass로 충분했다"고 썼는데, route_search로 바꾸고 나니 그 "충분했다"는 사실 weighted_hybrid_search가 BM25를 안 버렸기 때문이었다는 게 드러남.

**✅ [수정 완료] 쿼리 디콤포지션 구현·배선함**: `index_text.decompose_query()`/`decompose_and_route_search()` 신규 추가. gpt-4o로 질의를 성격별 하위질의로 분해(이 질의는 실제로 "abstract: 인사이트 도출" + "keyword_specific: 건설업종 최고 수익률 기업 추출" 2개로 정확히 쪼개짐, 실측 확인) → 하위질의마다 각자 전략(keyword_specific=hybrid RRF, abstract=HyDE/MQE)으로 검색 → **라운드로빈으로 인터리빙**해 합침(첫 시도는 순위기반 RRF 합산이었는데, abstract 하위질의가 코퍼스 전반에 점수를 흩뿌리면서 keyword_specific의 1위 결과가 오히려 희석되는 걸 실측으로 발견 — 라운드로빈으로 교체해 각 하위질의의 1위가 최상위 슬롯에 무조건 들어가게 고침). 결과: 도표3(건설업종)이 다시 상위권(2위)으로 복귀. `run_investment_opinion_demo.py`의 검색 단계를 route_search에서 이걸로 재교체.

**✅ [수정 완료] "기업명 및 티커" 자동 연결 — `company_entity_linking.py` 신규**: dense 대신 `pykrx.stock.get_market_ticker_name()`(로그인 불필요, 199/199 성공)으로 KOSPI200 199종목의 정확한 한글명을 캐시(`KOSPI200_output/kospi200_korean_names.json`)해두고, PDF 원문 텍스트에 그 이름이 등장하는지 정확 매칭한다. **오탐 함정 발견·수정**: "GS"(GS지주)가 "GS건설" 안에서, "동서"(동서식품)가 "아이에스동서" 안에서 오탐되는 걸 실측으로 발견 — 3자 이하 이름은 앞뒤가 한글로 안 이어질 때만(공백/구두점) 인정하도록 고쳐 오탐 0으로 해결(진짜 매칭은 "한샘"처럼 유지). Construct 실행 결과 **7개 기업 정확 매칭**(현대건설/한샘/삼성E&A/삼성물산/대우건설/한일시멘트/DL이앤씨) → `financial_summaries`/`company_profile_chunks`에서 실제 재무 요약을 PK로 직접 조회(검색 아니라 정확 조회라 오류 여지 없음)해 최종 프롬프트에 결합. 문서 근거 검색과 **ThreadPoolExecutor로 병렬 실행**(사용자 지적 "DB를 두 번 병렬로 검색해" 반영).

**✅ [수정 완료] 최종 생성 모델 업그레이드**: `gpt-4o-mini` → `gpt-4.1`(컨텍스트 ~1M, 더 강한 추론).

**🔴 그런데도 최종 재실행 결과 "1위 기업" 답이 또 틀렸다 — 새로운 원인, 정직하게 기록**: 검색은 이제 정확히 도표3을 상위권에 올렸고, DB 매칭도 정확했는데, 최종 답변이 "가장 높은 수익률은 금호건(5.2)"라고 답함 — 이것도 틀렸다(정답은 여전히 IPARK현대산업개발 +1.3%). 원인은 이번엔 로직이 아니라 **원본 OCR 데이터 자체의 결함**: 도표3 원문 OCR 텍스트가 "코오롱글로벌 (5.9) 금호건5.2) HL D&I (2.7)"인데, "금호건설(5.2)"에서 "설("이 통째로 유실돼 "금호건5.2)"로 깨져 들어왔다(PP-OCR의 박스 병합 오류로 추정). 여는 괄호가 없어 내 부호 정규화 정규식(`\((\d+)\)`, 여닫는 괄호 쌍 필요)이 이 항목을 못 잡았고, 그래서 LLM이 이걸 괄호 없는 양수로 읽었다 — 회사명 자체도 "금호건"으로 잘못 나와 실재하지 않는 이름이다(그래서 `company_entity_linking`이 매칭 안 한 것도 오히려 정합적 — 매칭 안 됐다는 게 "이 이름은 우리가 아는 진짜 기업이 아니다"라는 신호였는데 생성 단계가 그 신호를 활용 안 함). **이건 로직 버그가 아니라 원본 이미지 OCR 품질 문제라 리랭킹/디콤포지션으로 못 잡는 새로운 종류의 실패 — §"남은 과제"에 기록.**

**⚠ 추가로 발견한 세부 이슈**: 납기에서 `route_search()`가 실제로 탄 MQE 경로(ndcg 0.693)가 독립적으로 부른 `MQE(BM25 제외)`(ndcg 0.871)보다 낮음. 원인 추정: [44]가 지연 단축을 위해 "분류+하위질의 생성"을 `_classify_and_expand()` 호출 1번으로 합쳤는데, 이게 `mqe_search()`가 따로 쓰는 전용 하위질의 프롬프트보다 이 문서에서 품질이 낮은 하위질의를 만든 것으로 보임 — 라우팅 방향(MQE가 맞다) 자체는 옳았지만 [44]의 지연 최적화가 이 케이스에선 품질을 일부 희생시켰을 가능성. 재검토 필요.

**⚠ 오늘 발견·수정한 동시성 버그**: 3브랜치(text/table/image)를 `ThreadPoolExecutor`로 동시 실행하니, 텍스트 브랜치의 `BGE-m3-ko` 임베딩과 표 브랜치의 `TATR`이 **둘 다 자동으로 Apple MPS에 올라가 같은 시점에 다른 스레드에서 GPU 커널을 동시 제출**하면서 TATR 결과가 예외 없이 조용히 틀어지는 걸 실측으로 확인(같은 표가 격리 실행 27개 canonical 매칭 → 동시 실행 시 2개로 저하). **TATR을 CPU로 고정**해 해결(MPS 이득이 원래 9%뿐이라 손해 작음, [37] 참고). → **동시 실행 도입 시 반드시 함께 점검해야 할 클래스의 문제**로 팀에 공유 필요(다른 로컬 모델을 추가로 동시 실행에 얹을 때도 같은 문제가 재발할 수 있음).

**연결 상태 최종 점검**: `entity_fusion.py`/`index_text.py`/`ingest_queue.py`는 이번 세션에서 로직을 수정하지 않았고, 표/이미지 브랜치의 새 출력이 기존 인터페이스(`from_text_chunks`/`from_table_records`/`from_image_cards`)를 그대로 타고 들어가는 것만 확인 — **연결 안 된 부분 없음.**

---

## 6. 최종 생성 LLM 평가

**모델**: `gpt-4o-mini`(생성) + `gpt-4o-mini`(citation-check 검증) — `pdf_pipeline/citation_check.py`.

**설계**: 답변 생성 → `find_unsupported_numbers()`로 답변에 등장한 3자리 이상 숫자가 컨텍스트에 실재하는지 확인 → 없으면 피드백과 함께 최대 2회 재생성. `_retryable_create()`(tenacity, 429/5xx/타임아웃만 재시도)로 API 레벨 장애도 보호(오늘 배선 안 돼있던 걸 수정 — §부록 참고).

**Construct 실행 실측**:
- 1회 생성으로 통과(`unsupported_numbers: []`), 총 6.3s.
- **인용 정확도**: 답변에 등장한 수치(14.3%, 13.2%, 7.6%, 1.3%, 11.6%, 0.6%p, 11.7% 등)가 전부 컨텍스트에 실재 — citation-check가 정상 작동.
- **⚠ 그러나 citation-check를 통과한 답변이 처음엔 틀렸음**: gpt-4o-mini가 "값(N)" 괄호(한국 증권 리포트 관례상 **음수**)를 그냥 큰 숫자로 읽어 **GS건설(-14.3%, 실제로는 가장 큰 하락)을 "가장 높은 수익률"**이라고 답한 오류를 실측으로 발견. citation-check는 숫자가 컨텍스트에 "존재하는지"만 보고 부호는 안 봐서 이 오류를 못 걸러냄(모듈 자체 문서화된 한계: "단위 오귀속... 못 잡는다").
  - **1차 시도(프롬프트에 "괄호=음수" 지침 추가)**: 실패 — gpt-4o-mini가 지침을 안정적으로 안 지킴(동일하게 GS건설을 오답으로 지목).
  - **2차 시도(데이터 정규화)**: `block_type="chart"` 카드의 `embed_text`에서 `(N)` → `-N`으로 **인제스트 시점에 결정적으로 치환**. → 정답(IPARK현대산업개발, +1.3%)으로 정정 확인.
  - **교훈**: LLM의 즉석 해석(프롬프트 지침)에 기대는 것보다, 애매한 표기 관례는 **데이터 단계에서 정규화**하는 쪽이 훨씬 안정적 — 이번 사례가 그 근거.

**정정 후 최종 답변(발췌)**:
> "...건설업종에서 주간 수익률이 가장 높은 기업은 **IPARK현대산업개발**로, 1.3% 상승세를 기록하였습니다... 반면, **GS건설**은 -14.3%로 가장 큰 하락률을 보였으며..."

부가로 건자재업종 차트(도표4)에서 "한샘 11.7%"까지 자발적으로 인용 — 여러 소스(text+image) 근거를 실제로 종합하는 것도 확인됨.

---

## 베이스라인 vs 현재 고도화 비교표

| 단계 | 베이스라인 | 현재(고도화) | 실측 개선 |
|---|---|---|---|
| **페이지 처리** | 전 페이지 Docling/VLM 고비용 변환 | PyMuPDF 신속검사 → YOLOv11n(easy)/MinerU(hard) 분기 | 불필요한 고비용 변환 회피(설계 목표) |
| **표 파서** | baseline(무구조 pdfplumber) | enhanced_v5 하이브리드 게이트 | recall 0.672→**0.835**(p<0.0001), 속도 동급 |
| **표 파서** | docling / MinerU 표 엔진 | enhanced_v5 하이브리드 게이트 | 속도 **9.5~11.4배** 빠름, recall 근소 우세(p≈0.10) |
| **표 파서(오늘)** | TATR-always(12/14 표에 매번 TATR) | v5+3단계 TATR 안전망(1/14 표에만) | LGCNS 실측: canonical 매칭 43개(TATR-always와 동일) + **속도 6.73s→5.21s** |
| **검색** | dense-only | dense+BM25 하이브리드 | recall@1 **86.7%→93.3%**(15개 factoid 질의) |
| **쿼리 분류기** | 규칙 기반 | gpt-4o | 정확도 **80%→92%**(25개 라벨셋) |
| **추상형 질의 검색(다기업)** | HyDE | MQE | ndcg **0.403~0.526→0.792**(납기, 3기업) |
| **추상형 질의 검색(단일기업)** | MQE | HyDE | ndcg **0.749→0.967**(LGCNS, 1기업) |
| **이미지 브랜치 전량 판정** | VLM(Qwen3-VL) 전량 | MinerU 원스톱(OCR+분류기) | 249.7s→**4.5s**(55배), F1 95.9%→76.1%(비전LLM 판정 없이 상한 포기) |
| **3브랜치 실행** | 순차(sum) | ThreadPoolExecutor 동시(max) | 오늘 실측(Construct): 27.5s→**10.8s**(2.55배), (LGCNS): 20.9~21.0s→**8.2~8.6s**(2.4~2.5배) |
| **생성 API 안정성** | 재시도 없음 | tenacity 재시도(429/5xx/타임아웃) | 오늘 배선 안 돼있던 걸 수정(회귀 버그) |
| **차트 수치 해석** | LLM 즉석 해석(괄호=음수 프롬프트 지침) | 데이터 단계 정규화 | 정답률: 오답(GS건설 오지목)→**정답**(IPARK현대산업개발) |
| **검색 라우팅(4문서 평균, 오늘 확대검증)** | dense-only | 라우팅(신, 엔티티인식) | ndcg **0.554→0.848**, mrr **0.317→0.875** |
| **검색 라우팅(4문서 평균)** | 라우팅(구, 항상 MQE) | 라우팅(신, 엔티티인식) | ndcg **0.754→0.848**, mrr **0.625→0.875** |
| **검색 라우팅(오늘 데모 배선, Construct 복합질의 단일 사례)** | `weighted_hybrid_search`(고정 hybrid) | `route_search`(신 라우터) | ⚠ 평균은 이겼지만 이 복합질의는 **역전(정답→오답)** — §5 "route_search 오답" 참고, 평균 최고≠매 사례 최고 |
| **이미지 OCR/VLM 캐싱** | 캐시 없음(구현은 있었으나 미배선) | content-hash 캐시 배선 | LGCNS 재실행: 7.4s→**4.1s**(OCR 엔진 로드까지 스킵) |
| **DB evidence 재조회** | 매 호출 DB+BM25 재구축 | (pdf_id,ticker) 프로세스 캐시(TTL 60s) | 1.0s→**0.0000s**(히트), 무효화 후 0.74s(정상) |
| **기업명→티커 자동 해석(오늘 실측만, 미도입)** | (미구현) | dense 검색(company_profile_chunks) | 시도해봄 — 4건 중 1건만 부분 히트, **도입 보류**(§4 참고) |

---

## 부록 — 이번 세션에서 고친 것 (팀원 참고용)

1. `citation_check.py`: `_retryable_create()` 정의만 있고 안 쓰이던 회귀 버그 수정 — 이제 실제로 재시도 보호됨.
2. `run_investment_opinion_demo.py`: 3브랜치 순차→`ThreadPoolExecutor` 동시 실행, PDF/쿼리 파라미터화(`main(pdf_path=, pdf_id=, ticker=, query=, add_structured_metadata=, sector=)`), ONESTOP_CARDS_PATH 경로 버그 수정, 차트 OCR 괄호-음수 정규화 추가.
3. `table_processing/row_parser.py` / `run_table_metadata_pipeline.py`: TATR-always → v5 하이브리드 게이트 + 3단계 TATR 안전망(complexity=complex이고 word-clustering도 부실해 보일 때만), TATR CPU 고정(MPS 동시성 버그 수정).
4. `table_processing/adaptive_table_router.py`: `detect_and_route(pdf_path=...)` 명시적 인자 추가 — 여러 PDF_PATH 전역이 어긋나던 조용한 버그 원천 차단.
5. `streamlit/main.py`: `build_records()` 시그니처 변경(tatr_model/tatr_processor 제거)에 맞춰 `get_tatr_model()` 호출부 동기화(안 고쳤으면 팀원 대시보드가 깨질 뻔함).
6. `.gitignore`: `pdf_pipeline/data/`(원본 PDF 사본 + MinerU 파싱 결과 + onestop 카드, 재생성 가능·용량 증가 예상) 추가.
7. `image_processing/s2_onestop_mineru.py`: `ocr_crop`/`chart_table_extract`/`narrative_from_table`에 이미 있던(안 쓰이던) content-hash 로컬 캐시 배선.
8. `entity_fusion.py`: `load_evidence_from_db()`에 `(pdf_id,ticker)` 프로세스 메모리 캐시(TTL 60s) + `invalidate_evidence_cache()` 추가.
9. `text_processing/golden_set_event_insight.json`: 핀터멘털(entity_count=0 유형) 골든셋 추가해 라우팅 검증 표본을 3→4개 문서로 확대.
10. `run_investment_opinion_demo.py`: 검색 단계를 `entity_fusion.weighted_hybrid_search()`→`route_search()`→최종적으로 `index_text.decompose_and_route_search()`로 교체 + `precompute_entity_count()` 추가 + 기업 DB 매칭 병렬 실행 배선 + 최종 생성 모델 gpt-4.1로 교체.
11. `text_processing/index_text.py`: `decompose_query()`/`decompose_and_route_search()` 신규 추가([52]) — 복합 질의를 성격별 하위질의로 분해 후 라운드로빈 융합.
12. `pdf_pipeline/company_entity_linking.py`: 신규 파일 — PDF 근거 텍스트를 KOSPI200 199종목 한글명(pykrx 캐시)과 정확 매칭해 `financial_summaries`/`company_profile_chunks`에서 재무 요약을 직접 조회.
13. `KOSPI200_output/kospi200_korean_names.json`: 신규 캐시 파일 — `{ticker: 한글명}` 199개(pykrx 1회 생성).
14. `run_investment_opinion_demo.py`: **생성 모델 파라미터화** `main(..., gen_model="gpt-4.1")`(기본값 유지·무회귀) + stage8 `model=gen_model` 사용 + 프롬프트에 "DB 원본 숫자 자릿수 그대로 인용"(citation 재시도 꼬리 감소) 지침 추가. 근거: §7 Construct A/B(gpt-4o-mini 2.2배 빠름·동급 정확).
15. `pdf_pipeline/final/work/construct_ab.py` + `results_construct_ab.json`: Construct 성능 A/B 하네스(재파싱 없이 Supabase evidence 로드 → 검색전략×생성모델 지연·정확성 측정) 신규.

## 7. 성능 평가 지표 도입 + Construct 최종 A/B (지연·정확성) — 재일

**목적**: `20260721_industry_362851000.pdf`(Construct)에 대해 성능 평가 지표를 도입하고, 재파싱 없이(Supabase `document_evidence` 238건 로드) **검색 전략 × 생성 모델**을 A/B해 병목을 특정하고 지연을 줄인다. 하네스: `pdf_pipeline/final/work/construct_ab.py` → `results_construct_ab.json`. 쿼리는 §머리말과 동일(복합: "인사이트 도출"+"건설업종 최고 수익률 기업").

### 7.1 도입한 평가 지표
- **검색 정확성**(정답 프록시): 질의가 "건설업종"을 물으므로, **도표3(건설업종 주간수익률) image evidence가 도표4(건자재) image보다 위**로 랭크되면 correct. (도표3이 밀리면 §5의 오답 연쇄로 이어짐)
- **생성 정확성**: 최종 답이 건설업종 1위=**IPARK현대산업개발**을 지목하는가(§6 정답), citation-check `attempts`/`unsupported_numbers`.
- **지연**: 단계별 + 검색+생성 end-to-end.

### 7.2 A/B 결과 (Construct, 실측)

**(A) 검색 전략** — evidence 238건 로드 0.65s, entity_count=8:

| 전략 | 지연 | 도표3(건설,img) 순위 | 도표4(건자재,img) 순위 | correct |
|---|---:|---:|---:|:--:|
| `weighted_hybrid_search` | **0.05s** | 2 | 3 | ✅ |
| `route_search`(단일분류) | 1.93s | 6 | **1** | ❌(역전) |
| `decompose_and_route_search`(현 데모) | 2.78s | 2 | 4 | ✅ |

→ **route_search만 도표4를 1위로 올려 역전**(abstract 경로가 BM25를 버려 "건설업종" 문자 매칭을 잃음, §5와 일치). `weighted_hybrid`는 BM25를 유지해 **정확하면서 55배 빠름**(0.05s). 단 §5 4문서 평균에선 순수 abstract 질의에서 routing이 이기므로 **weighted_hybrid가 항상 우월한 건 아님** — 이 복합/키워드 포함 질의에선 우연히 최적. 어쨌든 **검색은 병목이 아니다**(최대 2.78s ≪ 생성).

**(B) 생성 모델** (검색은 decompose 고정, 동일 컨텍스트 10.8K자, 기업DB매칭 7건):

| 모델 | 지연 | attempts | IPARK 정답 |
|---|---:|---:|:--:|
| **gpt-4o-mini** | **6.58s** | 1 | ✅ |
| gpt-4.1(현 데모) | 14.74s | 1 | ✅ |

→ **gpt-4.1이 gpt-4o-mini 대비 2.2배 느린데 정확성 이득은 없음**(이 질의에선 둘 다 정답, 재시도 0). §6/§남은과제9의 "gpt-4.1 교체 후 재시도 잦아 48s"의 비용이 이 A/B에서 지연으로 확인됨.

**검색+생성 end-to-end**:

| 경로 | 지연 |
|---|---:|
| 현 데모(decompose + gpt-4.1) | 17.52s |
| opt_A(decompose + gpt-4o-mini) | **9.36s** (−47%) |
| opt_B(weighted_hybrid + gpt-4o-mini) | **6.63s** (−62%) |

### 7.3 병목 판정 + 지연 개선(구현 완료)

**병목 = 생성 단계**(6.6~14.7s, §6에선 재시도로 최대 48s). 검색(≤2.78s)·로드(0.65s)는 부차적. BGE 콜드로드 10.4s는 프로세스 1회성(질의당 아님).

**구현한 개선 2건**(안전·기본값 불변):
1. **생성 모델 파라미터화**: `main(..., gen_model="gpt-4.1")` 추가(기본값 유지 → 무회귀). 지연 민감 호출은 `gen_model="gpt-4o-mini"`로 **2.2배 단축**(정확성 동일, 위 A/B 근거). 데모 stage8이 `model=gen_model` 사용.
2. **프롬프트 하드닝(재시도 꼬리 감소)**: 생성 프롬프트에 "기업 DB 원본 숫자는 단위 변환·반올림 없이 자릿수 그대로 인용" 지침 추가. §남은과제9의 원인(gpt-4.1이 원단위 금액을 억/백만으로 재표기 → citation-check가 근거없는 숫자로 오인 → 불필요 재생성)을 데이터가 아니라 지침으로 선제 차단(모델 불변, 오직 재시도만 줄임 → 다운사이드 없음).

**권장(기본값 변경은 팀 확인 후)**: 이 질의에선 gpt-4o-mini가 동급 정확·2.2배 빠름. 다만 1개 질의 근거라 **여러 질의로 확대 검증 후** 기본 모델 하향을 결정할 것(§6가 gpt-4.1을 "강한 추론" 목적으로 올린 맥락 존중). 지금은 **레버(gen_model)만 제공**.

### 7.4 파이프라인 진행 중 발견한 병목/이슈 체크
- **생성 재시도**가 최대 병목(위). → gen_model 레버 + 프롬프트 하드닝으로 완화.
- **route_search 단독은 이 복합질의에서 오답**(도표4 1위) — 데모는 이미 decompose로 교체돼 있어 안전하나, route_search를 직접 쓰는 `run_cband_full_pipeline_test.py`는 이 함정에 노출됨(참고).
- **BGE 콜드로드 10.4s**: 서비스화 시 모델 프리로드(워밍업)로 질의 지연에서 분리 필요(현재 데모는 매 프로세스 1회 지불).
- 검색 자체는 병목 아님 — decompose의 gpt-4o 분해 호출(≈2s)이 대부분. 키워드 포함 질의는 `weighted_hybrid`(0.05s)로도 정확하니, **질의 성격별로 decompose 스킵 조건**을 두면 검색 지연을 더 줄일 여지(향후 과제, §남은과제).

---

## 8. 텍스트겹침 OCR 손상 수정 ✅ + 건자재 질의 A/B + 메타/엔티티 평가 + 검색 냉정분석 — 재일

**대상/질의**: Construct(`362851000`) 동일 PDF. 새 질의 = **"이 PDF 기업 인사이트 도출 + 건자재업종 종목 중 주간 수익률 최고 기업"**. 골든셋은 PDF 텍스트레이어(권위)를 직접 읽어 제작(`pdf_pipeline/final/ground_truth_construct.json`). 재파싱 없이 Supabase evidence 로드 → A/B(`final/work/construct_ocr_fix_ab.py`, `mqe_ab.py`, `construct_ab.py`).

### 8.1 골든셋 (PDF 직접 검증)
- **도표3 건설업종 1위 = IPARK현대산업개발 +1.3%**(유일 상승). 금호건설 실제 **-35.2%**(최저).
- **도표4 건자재업종 1위 = KCC글라스 +2.5%**. 한샘 실제 **+1.7%**.

### 8.2 ✅ [해결] "텍스트 겹침" 차트 OCR 손상 수정 (금호건설·한샘 둘 다)
차트 이미지에서 텍스트가 겹쳐 PP-OCR이 깨지는 문제 — 두 건이 실제로 stored image evidence에 있었음:
- `...코오롱글로벌 -5.9 **금호건5.2)** HL D&I -2.7` — "금호건설(35.2)"의 "설(3" 유실 → 회사명 손상 + 여는괄호 소실 → +5.2 오독.
- `...KCC글라스 2.5 ... **한샘 11.7** ...` — "1.7"에 '1' 중복 → 11.7(축 최대 +4 **초과=off-chart**) → "건자재 최고"로 오답.

**수정 3종**(`run_investment_opinion_demo._normalize_chart_card_signs(cards, pdf_path=)`에 배선, 데이터 단계 결정적 처리):
1. `(N)` 완전괄호 → `-N` (기존).
2. **`N)` 닫는괄호만(여는괄호 유실) → `-N`** — 금호건설 부호(하락) 복원 → "최고"로 못 뽑힘.
3. **텍스트레이어(권위) 대조** — 이미지 OCR 숫자가 PDF 원문 페이지 숫자집합에 없으면 손상값으로 보고 `[OCR손상]`으로 치환(수치 제거). 한샘 "11.7"은 원문에 없고(원문엔 1.7) → 제거되어 "최고" 근거로 못 쓰임. **핵심: 베이스라인의 강점(깨끗한 텍스트레이어)으로 파이프라인의 약점(이미지 OCR 손상)을 교정.**

**실측(Construct, gpt-4o-mini N=4)**: 건자재 최고 질의에서 **한샘 오답픽 corrupted 1/4 → fixed 0/4**(손상값 11.7이 제거돼 KCC글라스가 정답으로 선택됨). ✅
**주의(X 잔여 리스크)**: 수정(3)은 PP-OCR과 PyMuPDF 반올림 표기가 다르면 정상값도 제거될 위험 — 이 문서군엔 깨끗했으나 여러 문서 오탐률 측정 필요(남은과제 1).

### 8.3 건자재 질의 결과 (corrupted vs fixed)
| | 정답(KCC글라스) 픽 | 오답(한샘 11.7) 픽 |
|---|---|---|
| corrupted(현행) | 1/4 | **1/4** |
| **fixed** | 2/4 | **0/4** ✅ |
→ 손상값 제거로 오답 근본 차단. (정답픽 2/4는 표현 다양성 탓, 오답 0이 핵심)

### 8.4 텍스트/이미지/테이블 메타·엔티티 추출 품질 (vs 골든셋) + 베이스라인 델타
저장된 `document_evidence.metadata.structured_metadata` 실측:
| 브랜치 | 추출 엔티티 | 메타데이터 | 판정 |
|---|---|---|---|
| **image** | **25개(GT 21/21 전부** + 도표1~5 제목) | image_type/described_content/key_values/time_period | ✅ 최강(차트 라벨 종목 다 잡음) |
| text | 13개(본문 언급 종목 + 출처) | section_path + entities/topic/sentiment | ✅ 양호 |
| **table** | **0개(entities_mentioned 빈값)** | table_idx/canonical_field만 | X **엔티티 갭**(표에 종목 있으나 미추출) |
**베이스라인 델타**: baseline(raw text sidefield 추출) **9/21=0.43** vs pipeline(image 브랜치) **21/21=1.00**. baseline은 차트-라벨-only 종목(KCC글라스·동화기업·한샘·금호건설·코오롱글로벌 등)을 **전부 놓침** → **이미지 브랜치가 파이프라인의 엔티티 핵심 가치**(2.3배).

### 8.5 MQE 결합 vs 전용 A/B (§5 "재검토 필요" 항목)
Construct 건자재 질의(abstract)로 재현:
| 방식 | ndcg@8 | 하위질의 품질 |
|---|---:|---|
| 전용 `mqe_search` | 0.807 | 실적/목표주가/리스크/건자재비교 |
| 결합 `_classify_and_expand` | 0.807 | 실적/목표주가/리스크/건자재위치 |
→ **동률.** [44] 결합호출이 이 질의에선 전용 대비 품질 저하 **없음**. 핸드오프의 납기 갭(0.693 vs 0.871)은 **문서/골든셋 특이적**일 가능성 — 여러 문서 확대 전엔 "결합이 항상 나쁘다"고 볼 근거 부족(냉정하게: 1문서 재현 실패).

### 8.6 X [최근확인 병목/문제] 검색·인덱싱 냉정 분석
- **X route_search 단독 = 복합/키워드 질의서 오답**: 건자재 질의에서 도표4(건자재)를 image 1위, 도표3(건설)은 6위로 올림 — abstract 경로가 `use_bm25=False`로 "건자재/건설업종" 문자 매칭을 버림. decompose가 정답순서 복원(도표3 2위 vs 도표4 4위)하나 **2.78s**. `weighted_hybrid`(0.05s)는 BM25 유지해 이미 정확 → **키워드 포함 질의엔 decompose 오버킬**(§7.4/남은과제11).
- **X 생성 단계 병목**: gpt-4.1이 gpt-4o-mini 대비 **2.2× 느림(14.7s vs 6.6s)**, 이 질의군 정확성 이득 0(§7). → gen_model 레버·프롬프트 하드닝 제공(§7.3).
- **X 테이블 엔티티 갭**: 표 브랜치 structured_metadata.entities_mentioned가 빈값 → 표 속 종목이 엔티티로 안 남음(위 8.4). 표는 canonical/수치 위주라 그런데, 다기업 리포트에선 표 엔티티도 살리는 게 나음(남은과제 추가).
- **인덱싱**: BGE-m3-ko 콜드로드 10.4s는 프로세스 1회성(질의당 아님) — 서비스화 시 프리로드 필요.

---

## 9. 전체 파이프라인 연결/캐시 감사 + 추상질의 생성모델 재검증 + 표 엔티티 갭 수정 ✅ — 재일

### 9.1 DB 연결·기업매칭·캐시 — 전부 정상(연결 안 된 곳 없음)
| 점검 | 결과 |
|---|---|
| DB 테이블 | financial_summaries 199 / company_profile_chunks 199 / financial_chunks 1190 / document_evidence 3241 ✅ |
| **기업명→티커 매칭** | Construct **7건 정확**(현대건설 000720.KS·한샘 009240.KS·삼성E&A·삼성물산·대우건설·한일시멘트·DL이앤씨) ✅ |
| **db_context** | **8,643자 실제 재무요약**이 생성 프롬프트에 실림(입력 6,307토큰으로 확인) ✅ |
| evidence 캐시 | 1차 0.70s → 2차 **0.0000s(히트)** → 무효화후 0.60s ✅ |
→ **"DB 미연결/캐시미스" 없음.** 유일한 실 연결버그가 아래 표 브랜치(발견·수정).

### 9.2 ✅ [해결] 표 브랜치 엔티티 갭(entities 0건)
**근본원인(코드)**: ① `entity_fusion.from_table_records()`가 metadata에 `{canonical_field, table_idx}`만 넣고 **structured_metadata를 안 실음**(text/image는 실음) ② 데모가 `record_type=="table_metadata"` 레코드를 **버림** ③ LLM `extract_table_metadata`가 이 표들에서 entities_mentioned를 **빈값**으로 줌.
**수정(3중)**: ① from_table_records가 structured_metadata 부착 ② 데모가 table_metadata를 table_idx로 행에 매핑 ③ **행 content에서 KOSPI200 기업명 결정적 매칭**으로 entities 채움(LLM 빈값 보완).
**실측**: 표 223행 중 entities 채워진 행 **0 → 19**(현대건설/대우건설/삼성E&A/삼성물산/DL이앤씨/한샘/한일시멘트). ✅

### 9.3 "4o-mini가 왜 4.1보다 좋냐" — 추상질의로 재검증(DB 연결 상태)
| 질의(추상) | gpt-4o-mini | gpt-4.1 |
|---|---|---|
| 핵심 인사이트 도출 | 11.8s, out **890토큰** | 17.2s, out **2,153토큰** |
| 핵심 요약·분석 | 7.3s, out **715토큰** | 23.1s, out **2,133토큰** |
- **DB 컨텍스트 정상 연결(8.6K자)** 상태인데도 4.1은 **출력토큰 2.4~3배·지연 1.5~3배**, 품질 우위 불명확(judge: insight 무승부, summary mini 우세/무승부).
- 해석: 근거(차트 수치+DB 재무)가 **구체적·명시적**이라 강한 추론이 필요 없는 질의군 → 4.1의 이점이 안 드러나고 장문화만 됨. "mini가 더 좋다"가 아니라 **4.1의 추가비용 대비 이득 없음**.
- **X 정직한 한계**: LLM-judge 위치편향 관측(insight 양쪽 "A") — 품질신호 약함. 확정엔 루브릭/사람 평가 필요.

### 9.4 X [병목·토큰] 어디서 새는가 + 최적화
| 항목 | 실측 | 최적화 |
|---|---|---|
| **생성(최대 병목)** | 4.1 out 2.1K토큰·17~23s | `gen_model="gpt-4o-mini"` 시 out 0.7~0.9K·7~12s (**토큰 2.4~3×↓, 지연 1.5~3×↓**) — 레버 제공 |
| 입력 컨텍스트 | 6.3K토큰(evidence + db_context 8.6K자≈2.1K토큰) | db_context가 매칭 7사 **전체 재무요약 통짜** → 질의 관련 기업/핵심지표만 주입하면 대폭 절감(향후) |
| 검색(decompose) | 2.78s(gpt-4o 분해 ≈2s) | 키워드 포함 질의는 weighted_hybrid(0.05s)로도 정확 → **성격 게이팅으로 decompose 스킵** |
| citation 재시도 | 4.1서 DB 큰 수 재표기→재시도↑ | 프롬프트 하드닝(자릿수 그대로) 적용(§7.3) |
| BGE 콜드로드 | 10.4s | 프로세스 1회성 — 서비스화 시 프리로드 |

### 9.5 라우팅(분류→HyDE/hybrid)이 "성능 낮냐" — 냉정 정리
- `weighted_hybrid_search`는 **모든 질의에 동일한 고정 하이브리드**(dense 0.7+BM25 0.3, 라우팅 없음). route_search/decompose가 **gpt-4o 분류 → 추상=HyDE/MQE(BM25 제외)·키워드=hybrid RRF**로 가름.
- 라우팅 방향 자체는 §5 4문서 평균에서 최고(ndcg 0.848 vs 고정 hybrid 0.428~0.470)로 **옳음**. **약점은 두 지점**: (a) **분류 오판** — route_search 단독이 복합/키워드 질의(건자재)를 abstract로 몰아 BM25를 버려 도표3/4 역전(§8.6), (b) **키워드 질의 decompose 오버킬**(2.78s vs 0.05s). → 성격 게이팅이 다음 개선점.

---

## 10. 주간 수주 공시 표 복구 ✅ + 퓨전/라우팅 골든셋 증명 + RAGAS 생성평가 + 스키마 재설계 ✅ — 재일

전체 보고서: `pdf_pipeline/final/검색퓨전_라우팅_생성품질_실험.md`
골든셋: `pdf_pipeline/final/golden_set_construct_routing.json` — **PDF를 직접 읽어 수작업 제작**한 18질의(키워드형/하이브리드형/추상형 각 6).
산출물: `results_fusion_routing_ab.json`, `results_policy_eval.json`, `results_ragas.json`, `results_flatten_detector_ab.json`, `results_schema_fix.json`
하네스: `work/diag_order_table.py`, `work/ab_flatten_detector.py`, `work/fusion_routing_ab.py`, `work/policy_eval.py`, `work/gen_for_ragas.py`, `work/ragas_eval.py`, `work/ab_schema_fix.py`, `work/reingest_construct.py`

### 10.1 ✅ 주간 수주 공시 표가 왜 안 뽑혔나 — 3중 원인, 전부 수정

원인 ①: `row_parser._word_clustering_looks_flattened()`가 **`cells[0]`의 숫자만** 검사 → 이 표는 숫자가 **라벨 쪽에** 뭉쳐서 탐지 실패 → TATR 승격 안 됨(TATR을 강제 실행하면 정확히 파싱함을 `diag_order_table.py`로 확인). **수정**: (b)라벨뭉침 (c)줄바꿈 고아행 신호 추가 + "TATR이 실제로 구조를 개선했을 때만 채택" 가드(`_mean_cell_count`).
원인 ②: 비재무 헤더(`날짜|건설사|수주 프로젝트|계약 금액`)가 `detect_wide_form`에서 버려져 컬럼명 소실 → "계약 금액" 토큰이 본문에 없어 검색 불가. **수정**: `_detect_column_headers()` 구조 기반 회수 + `컬럼명=값` 직렬화.
원인 ③: 행 단위 청크라 **최댓값 질의가 원리적으로 불가**(H4 "계약금액이 가장 큰 프로젝트" — 검색 9개 방법 전부 hit=0). 게다가 제목행 노이즈가 데이터행을 밀어냄. **수정**: 제목/헤더행 레코드 제거 + **표 단위 요약 청크(table digest)** 신설(`entity_fusion._table_digest_items`).

| 지표 | 수정 전 | 수정 후 |
|---|---|---|
| p5 수주표 구조(셀평균/고아율) | 0.86 / 0.43 | **7.0 / 0.12** |
| K2 "상도15구역 계약금액" | 표에서 못 찾음 | **rank 1** (ndcg 1.000) |
| H4 "가장 큰 수주" | **9개 방법 전부 실패** | **rank 1** (0.946) |
| 비용 | — | 문서당 +약 0.2s/표, TATR 최초 로드 ~4s. SmartPhone 회귀 0 |

X **잔결함**: TATR이 "세대당 금액"을 2컬럼으로 중복 인식(값 중복, 검색엔 무해). p4의 글자겹침 표는 TATR도 못 고침 → 가드가 word-clustering 유지.

### 10.2 퓨전/라우팅 — 골든셋으로 증명한 것

| 방법 | 키워드형 | 하이브형 | 추상형 | 전체 ndcg/recall | 지연 |
|---|---|---|---|---|---|
| dense_only | 1.000 | 0.962 | 0.539 | **0.834**/0.89 | 0.05s |
| **linear 0.7/0.3(현행 고정)** | 0.880 | 0.908 | 0.656 | **0.815**/**0.94** | 0.05s |
| linear 0.5/0.5 | 0.880 | 0.759 | 0.578 | 0.767/0.94 | 0.05s |
| **rrf k=60** | 0.794 | 0.816 | 0.577 | **0.708~0.729**/0.89 | 0.05s |
| hyde | 0.687 | 0.816 | 0.658 | 0.720/**1.00** | 3.04s |
| mqe | 0.939 | 0.676 | 0.455 | 0.690/0.89 | 1.59s |
| decompose+route | 0.794 | 0.770 | 0.362 | 0.642/0.89 | 1.97s |
| route(gpt-4o) | 0.794 | 0.763 | **0.271** | 0.609/0.83 | 0.92s |

- **선형가중 > RRF 증명됨** (0.815 vs 0.708~0.729). min-max로 스케일을 이미 맞춘 파이프라인에서 RRF는 점수 크기라는 정보를 버리는 쪽이 된다.
- **0.7/0.3 > 0.5/0.5** (0.815 vs 0.767) — 현행 가중치 선택은 타당.
- **BM25의 값어치는 recall** — dense_only가 ndcg는 0.019 높지만 recall 0.89 vs **0.94**, 추상형 0.539 vs **0.656**.
- X **라우팅이 고정 하이브리드를 못 이긴다**(0.609/0.642 vs 0.815/0.834), 지연은 2~4배. 손해가 두 군데: (a) 키워드 경로가 `fusion="rrf"`(`index_text.py:387`), (b) 추상 경로 MQE 하위질의가 구체적이라 p1 전략 서술 대신 표로 검색을 끌고 감.
- X **분류기가 2진이라 "하이브리드형"을 표현 못 함** — `classify_query_type_llm()`은 abstract/keyword_specific만 반환(`index_text.py:236`), 하이브리드형 6질의가 **전부 keyword_specific으로 흡수**됨.
- **한계**: N=18·문서 1건. 표 요약 청크 추가 전엔 route 추상형이 0.886으로 최고였다가 0.271로 무너졌다 — **순위가 인덱스 구성에 민감**하므로 "라우팅이 항상 나쁘다"가 아니라 "현 구성에선 이득 없음"이 정확한 서술.

### 10.3 RAGAS 생성평가 — 이전 결론 철회

| 지표 | gpt-4o-mini | gpt-4.1 |
|---|---|---|
| faithfulness(환각 없음) | 0.740 | **0.893** |
| factual_correctness | 0.332 | **0.409** |
| 논리 루브릭(1~5) | 2.22 | **3.00** |
| answer_relevancy / context_precision / context_recall | 0.309 / 0.502 / 0.741 | 0.310 / 0.482 / 0.741 |
| 지연 / 출력토큰 | **4.71s / 304** | 6.04s / 814 |

§9.3에서 pairwise judge로 "4.1의 품질 우위 불명확"이라 보고했으나 **그 judge에 위치편향이 있었다**. 포인트와이즈 채점으로 다시 재니 **4.1이 환각 21%↓·논리 35%↑로 우세**. → **남은과제 10(기본모델 하향)은 보류가 맞다.**
X **주의**: faithfulness는 "정확성"이 아니라 "컨텍스트 인용도"를 잰다 — K2에서 mini가 정답(14,367억원)을 냈는데 0.00, 4.1이 단위를 환각(→143억)했는데 0.83을 받았다. 두 지표를 같이 봐야 한다.
X **한계**: N=9. 생성 답변이 표 요약 청크 수정 **이전** 인덱스에서 나와 H4 문항 점수가 낡음. 재측정하려 했으나 **OpenAI 조직 쿼터 소진(429 exceeded quota)** 으로 중단.

### 10.4 ✅ 스키마/Structured Output 재설계

| 결함 | 수정 | 효과 |
|---|---|---|
| 3개 스키마 전부 `Field(description=)` **0개** — 정의가 프롬프트 불릿에만 존재 | 전 필드에 "어디서 찾아라/무엇을 넣지 마라/없으면 무엇을 반환하라" 명시 | 아래 종합 |
| **표 브랜치만 문서 컨텍스트 0** — YOLO bbox 내부 텍스트만 LLM에 감 | 문서 제목 + **표 위 캡션**(`CAPTION_LOOKUP_PT=40`) 주입 | 표 `entities_mentioned` **0/15 → 13/15(87%)**, `table_title`이 캡션에서 실제 회수, `time_periods`가 `["0"]` 쓰레기 → 실제 날짜 |
| `table_type_refined`가 자유 문자열(15표에서 7종 난립) | `Literal` 10종으로 고정 | 필터·집계 가능 |
| **무료 엔티티 경로 사망** — `index_text.py:714`가 최상위 `structured_metadata`를 읽는데 융합 인덱스는 `metadata.` 아래 중첩 | 두 위치 + 두 키명(`entities`/`entities_mentioned`) 모두 읽도록 수정 | 매 인제스트 LLM 폴백 호출 제거(캐시미스 해소) |
| `temperature` 미지정(기본 1.0) + system 메시지 없음 | `temperature=0` + 추출 전용 system 메시지 | 추출 재현성 확보 |

X **남은 결함**: p8~p10 투자의견 변동표는 `entities=['괴리율']`(회사명이 표 밖 헤더에 있음 — **§11에서 해결**). p4 글자겹침 표는 원문이 깨져 스키마로는 해결 불가.

---

## 11. ✅ 스텝차트 축 오독(TP 시계열 조작) — 원인 3중 규명 + 수정, 차트 VLM 실효성 검증 — 재일

사용자 제보: c밴드 리포트 답변에서 4개 기업 **전부** 목표주가 추세를 실제와 **정반대(하락)**로 서술.
하네스: `work/diag_cband_tp_table.py`, `work/diag_cband_retrieval.py`, `work/test_chart_vlm.py`
산출물: `results_cband_tp_diag.json`, `results_cband_retrieval.json`, `results_chart_vlm_test.json`

### 11.1 원인 — "추출 실패"도 "랭킹 실패"도 아니었다. **주소가 없었다**

| 확인 항목 | 결과 |
|---|---|
| 표 브랜치가 TP표를 뽑았나 | ✅ 뽑았다 — p3~6에 63행 적재(`26.5.19: ['BUY','70,000',...]`) |
| 표 행에 **회사명**이 있나 | ❌ **0건** — 행이 `날짜+BUY+숫자`뿐. 회사명은 표 bbox **바깥**에 있다 |
| 회사명으로 도달 가능한 근거 | ⚠️ **차트 이미지 카드 하나뿐** — OCR은 Y축 눈금 9개 + X축 날짜 9개 |
| 결과 | 개수가 같은 두 목록을 생성 모델이 1:1로 짝지어 **없는 시계열을 창작** |

`"케이엠더블유 목표주가"` 질의는 정답 표 행과 어휘·의미 어느 쪽으로도 매칭될 수 없어 **검색 후보조차 되지 못했다.** 이건 §10.4에서 관측한 "Construct p8~p10 투자의견표 entities=['괴리율']"과 **같은 뿌리**다.

부수 결함: 이 표들의 헤더가 **두 줄**(`날짜|투자의견|목표주가` + `평균|최고/최저`)이라 헤더 탐지(±1 폭 조건)를 통과 못 해 컬럼명이 버려지고, 그 두 줄이 `날짜: ['투자의견','목표주가']` 같은 **노이즈 행으로 적재**돼 있었다.

### 11.2 수정 3건

1. **표 캡션 회수를 항상 수행 + 2단 레이아웃 대응**(`_caption_above`) — 표 위쪽뿐 아니라 **같은 높이의 왼쪽 영역**도 본다(투자의견 변동표는 차트와 표를 좌우로 나란히 놓아 캡션이 표 '왼쪽'에 있음, 실측). 페이지 머리글(`통신장비 Overweight`)이 잡히지 않게 상단 80pt 제외, 숫자 토큰 3개 이상인 줄(앞 표 데이터/축 눈금)도 제외.
2. **캡션을 검색 본문에 배선** — 행 content 앞에 `[투자의견 변동 내역 및 목표주가 괴리율 케이엠더블유]`, 표 요약 청크 머리말에도 부착(`entity_fusion`).
3. **2줄 헤더 인식** — 폭 허용범위를 `-1 ~ +2`로 넓히고, 헤더 다음 줄이 숫자 없는 하위헤더면 흡수(노이즈 행 제거 + `목표주가` 컬럼명 확보).

**실측(로컬 BGE+BM25, OpenAI 호출 0회)** — 표/텍스트/차트카드가 함께 경쟁하는 조건:

| 질의 | 수정 전 | 수정 후 |
|---|---|---|
| 케이엠더블유 26.5.19 목표주가 | 3위 | **1위** |
| 케이엠더블유 목표주가 변동 이력 | **top-8 밖** | **1위** |
| 우리넷 목표주가 추이 | **top-8 밖** | **1위** |
| 케이엠더블유 투자의견/괴리율 | **top-8 밖** | **1위** |

수정 전에는 4개 질의 모두 **차트 카드(축 눈금)가 1위**였다 — 그게 조작의 직접 원인.

### 11.3 ✅ 차트 VLM(4a)이 실제로 이 문제를 푼다 — 실측

`s2_onestop_mineru.py`의 `[4] 차트분석(--with-chart-analysis)`은 **이미 구현돼 있고 기본 off**("초당 비용 큼"). 모델도 이미 로컬에 있다(`~/.cache/mineru_vlm/MinerU2.5-Pro-2605-1.2B`, 2.27GB). 4b(narrative, OpenAI)는 빼고 4a만 5개 차트에 단독 실행(`work/test_chart_vlm.py`, `work/test_chart_vlm_multi.py`):

| 차트 | 유형 | 정답값 회수 | 지연 |
|---|---|---|---|
| c밴드 케이엠더블유 목표주가 추이 | A. 스텝차트(값 라벨 없음) | **7/7** | 20.5s |
| c밴드 아이씨티케이 목표주가 추이 | A | **3/3** | 18.2s |
| c밴드 우리넷 목표주가 추이 | A | 2/3 (중간 단계 15,000 누락) | 21.9s |
| Construct 도표3 건설업종 주간수익률 | B. 값 라벨 막대차트 | **12/12** | 17.0s |
| Construct 도표4 건자재 주간수익률 | B | **7/7** | 15.7s |

**유형별 결론**
- **B(값 라벨 막대차트): 19/19 전부 정확.** 순위·비교·추세 판단에 그대로 쓸 수 있다.
- **A(스텝차트): 방향과 추세는 정확, 값은 근사.** 계열에 "~" 접두사를 붙여 근사값임을 스스로 표시하고(예: `~8,000`), 눈금선에 정렬된 스텝 값은 정확히 읽는다. 다만 우리넷에서 중간 단계 하나를 건너뛰었다 — **A유형은 추세 서술용으로는 충분하나 정확한 값은 표 근거를 써야 한다.**
- ✅ **기존 OCR 손상 2건이 VLM으로 함께 해결된다**: 겹친 텍스트로 `11.7`로 깨졌던 **한샘을 `1.7`로 정확히** 읽었고, `금호건5.2)`로 깨졌던 **금호건설도 `35.2` 값을 복원**했다.
- X **단, 금호건설은 부호를 놓쳤다** — 다른 음수는 전부 `(7.4)`처럼 괄호로 출력했는데 금호건설만 `35.2`로 나와, `_CHART_PAREN_NEGATIVE_RE`(괄호→음수) 규칙에 안 걸려 **+35.2로 남는다**. 차트분석을 켜더라도 이 항목은 여전히 "최고 수익률" 오답 위험이 있으므로, VLM 출력에도 텍스트레이어 대조(§8 수정 (3))를 그대로 적용해야 한다.
- **VLM은 서술문을 쓰지 않는다.** 4a는 차트→**표** 복원까지고, "추세/흐름 텍스트"는 그 표를 받아 쓰는 4b(`narrative_from_table`, OpenAI)가 만든다. `narrative`는 이미 `build_embed_text()`에 포함돼 있어 켜면 곧바로 검색된다.

**권고**: 차트 위주 문서(투자의견 변동표류)에는 `--with-chart-analysis`를 켤 것. 비용은 차트 1장당 15~22s(모델 로드 5.3s는 프로세스당 1회) — 문서당 차트 8장이면 약 2분 추가.
X 주의: 4b는 OpenAI 쿼터가 필요하고, 로컬 VLM은 GPU를 하나만 쓰므로 **스레드 병렬화는 위험**(TATR+BGE 동시성 버그와 같은 계열) — 속도는 동시 호출이 아니라 배치 추론으로 내야 한다.

### 11.4 ✅ VLM이 꺼져 있을 때의 안전장치

`_looks_like_axis_ladder()` — 차트 OCR 숫자가 **등간격 사다리**(축 눈금)면 embed_text에 "이 숫자들을 날짜와 짝지어 시계열로 해석하지 말 것" 경고를 붙인다. 값 라벨이 붙은 막대차트는 등간격이 아니라 안 걸린다(단위테스트: c밴드 스텝차트 True / Construct 도표3 False). 차트분석이 켜져 `chart_table`/`narrative`가 있으면 경고를 붙이지 않는다.

---

## 12. ✅ `[OCR손상]` 과잉 치환 수정 + 차트 VLM 기본 on + VLM 산출물 배선 — 재일

실사용 실행 로그에서 확인된 2건. 하네스: `work/diag_ocr_falsepos.py`, `work/ab_ocr_filter_fix.py`
산출물: `results_ocr_falsepos_diag.json`, `results_ocr_filter_fix_ab.json`

### 12.1 ✅ `[OCR손상]`이 정상 축 라벨을 지우고 있었다 (남은과제 13이 실제로 터짐)

증상: 차트 근거가 `35,[OCR손상] 쏠리드`, `24,[OCR손상] 24,10 … [OCR손상],[OCR손상] 26.04`처럼 범벅.

**원인 — 이미지 OCR이 아니라 대조 필터의 토큰화 불일치.** 텍스트레이어 대조(§8 수정(3))는 정규식 `\d+(?:\.\d+)?`을 쓰는데 **쉼표를 모른다.** PP-OCR이 마침표를 쉼표로 읽는 순간(`24.07` → `24,07`) 토큰이 `24`+`07`로 쪼개지는 반면, 텍스트레이어 쪽은 같은 값을 `24.07` **한 토큰**으로 잡는다. 조각은 원리적으로 매칭될 수 없어 멀쩡한 값이 삭제됐다.

| 검증 | 결과 |
|---|---|
| `'24.07'` → 토큰 | `['24.07']` (한 덩어리) |
| `'24,07'` → 토큰 | `['24','07']` (조각남) |
| p3 텍스트레이어에 `'07'` 존재? | **False** → 무조건 손상 판정 |

**수정 2건**
1. 양쪽 모두 쉼표를 품은 하나의 토큰으로 뽑고(`\d[\d,.]*\d|\d`), 구분자 표기 차이를 흡수한 **변형 집합**(쉼표 제거형, 쉼표↔마침표 교환형)으로 비교(`_number_variants`).
2. **전멸 방지 가드** — 한 카드의 숫자 중 절반 넘게 안 맞으면 그 페이지 텍스트레이어가 이 차트를 아예 안 덮는 것(래스터 차트)으로 보고 손상 판정을 통째로 건너뛴다(`_OCR_CHECK_MAX_MISS_RATIO=0.5`).

**양방향 단위테스트 통과**: 진성 손상(한샘 `11.7`, 원문에 없는 값)은 **여전히 `[OCR손상]`으로 잡히고**, 오탐(`24,07 24,10 25,01`)은 **그대로 보존**된다.

### 12.2 ✅ 차트 VLM 기본 on + 산출물이 검색에 도달하도록 배선

**배선 결함(더 중요)**: `build_embed_text()`가 `narrative`(4b 서술문)만 싣고 **`chart_table`(4a VLM이 복원한 실제 데이터 표)은 안 실었다.** 4b는 OpenAI 호출이라 쿼터/네트워크로 실패할 수 있는데, 그러면 **4a가 GPU를 써서 복원한 수치가 검색에 전혀 도달하지 못한 채 버려진다.** → `chart_table`을 `[차트 복원 데이터]` 블록으로 함께 싣도록 수정. 검증: narrative 없이도 `수정TP 70,000`까지 embed_text에 실림.

**기본값 전환**: `--with-chart-analysis` **off → on**(끄려면 `--no-chart-analysis`). 근거는 §11.3 실측(값 라벨 막대차트 19/19 정확, 스텝차트 추세 정확, OCR 손상 2건 동시 해결). 비용은 차트당 15~22초.
X 남은 제약: 4b 서술문은 여전히 OpenAI 필요(현재 쿼터 소진). 금호건설 **부호 누락**은 VLM으로도 안 고쳐져 텍스트레이어 대조가 계속 필요. 로컬 VLM은 GPU 하나를 공유하므로 **스레드 병렬화 금지**(배치 추론으로만 가속).

### 12.3 실사용 로그로 확인된 §10~11 수정의 정상 동작

같은 실행에서 이전 수정들이 의도대로 작동함이 확인됐다 — 축 눈금 경고가 붙어 **생성 모델이 차트 수치 사용을 스스로 거부**했고("지침에 따라 시계열로 해석하지 말 것 … 수치 분석에서 제외"), 표 근거로 우리넷 목표주가를 `10,000 → 15,000 → 25,000` **상승**으로 정확히 서술했다(이전엔 정반대인 하락으로 조작됨).
X 별건으로 관측: citation-check가 답변의 면책 문장("'150%' 같은 값은 사용하지 않았습니다")에 들어간 숫자를 근거 미확인으로 잡는 **오탐**이 있다 — 검사 대상에서 메타 문장을 제외하는 보완 필요.

---

## 남은 과제(다음 담당자용) — 우선순위순

1. **✅ [해결됨, §8 — 최근] "금호건설/한샘" 차트 OCR 손상(텍스트 겹침)** — `_normalize_chart_card_signs`에 (2)닫는괄호→음수 + (3)텍스트레이어 대조로 원문에 없는 OCR 숫자 `[OCR손상]` 제거를 배선. 실측 한샘 오답픽 1/4→0/4. **남은 리스크(X)**: 텍스트레이어 대조가 반올림 표기차로 정상값을 제거할 오탐 위험 — 여러 문서로 오탐률 측정 필요. **[아래는 원 기록]** ~~[최우선, 오늘 실측으로 확인된 실패, 로직 수정으론 해결 안 됨] "금호건설" 원본 OCR 데이터 손상.~~ 복합 질의 라우팅(디콤포지션)까지 고친 뒤에도 최종 답이 "1위=금호건(5.2)"로 틀림 — 원인은 도표3 원문 OCR이 "금호건설(5.2)"의 "설("을 통째로 유실해 "금호건5.2)"로 깨져 들어왔기 때문(PP-OCR 박스 병합 오류 추정). 여는 괄호가 없어 부호 정규화 정규식이 못 잡았고, 회사명도 존재하지 않는 "금호건"이라 `company_entity_linking`이 매칭도 안 함(오히려 이게 신호였는데 생성 단계가 활용 안 함). **후보 해법**: (a) 정규화 시 "숫자)"처럼 여는 괄호 없이 닫는 괄호만 있는 패턴도 음수로 처리(단, 오탐 위험 있어 검증 필요), (b) `company_entity_linking`이 매칭 안 된 차트 라벨은 "미확인 기업명"으로 프롬프트에 명시해 LLM이 그 항목을 답의 근거로 안 쓰게 유도, (c) 이런 깨진 라벨이 실제로 얼마나 흔한지 여러 문서로 먼저 측정.
2. **[해결됨, 참고용] 복합 의도 쿼리 라우팅.** route_search 단일분류가 복합 질의를 abstract로만 오분류해 BM25를 버리던 문제 — `index_text.decompose_and_route_search()`로 해결(위 §5 참고). 이 자체는 실측으로 정상 동작 확인됨(도표3이 다시 상위권 복귀) — 위 1번(OCR 손상)은 이것과 무관한 별개 문제.
3. **[해결됨, 참고용] 기업명→티커 자동 해석.** dense 검색은 부정확했음(4건 중 1건만 부분 히트) — `pykrx` 정확명 매칭(`company_entity_linking.py`)으로 교체해 해결, Construct 실측 7건 정확 매칭(§4/§5). 남은 리스크: 캐시(`kospi200_korean_names.json`)가 199종목뿐이라 KOSPI200 전체/타 시장 종목은 여전히 매칭 안 됨 — 필요시 티커 목록 확장.
4. 뉴스/감성분석: 헤드라인 수집(`data_collection/fetch_news.py`)까지만, 감성분석+기업메타DB 결합은 미구현.
5. 실시간 주가: PRD상 on-demand API 호출로 설계돼 있으나 이 데모엔 실제 연동 없음(이번 쿼리는 문서 내 "주간 수익률" 표를 써서 우회 가능했음 — "오늘 현재가" 같은 질의는 별도 API 연동 필요).
6. 이미지 브랜치 [4a/4b](차트→표+서술형 해석)는 이번 문서에서 OCR만으로 충분해 생략 — 축 눈금과 데이터가 헷갈리는 복잡한 차트에서는 필요할 수 있음.
7. 납기에서 발견된 `route_search()`의 결합호출 MQE(ndcg 0.693) vs 독립호출 `MQE(BM25 제외)`(ndcg 0.871) 품질 격차 — [44] 지연 최적화가 하위질의 품질을 일부 희생했을 가능성, 재검토 필요.
8. `streamlit/main.py::answer_question()`이 financial_chunks/company_profile_chunks/document_evidence 세 테이블을 순차 조회 — ThreadPoolExecutor로 병렬화하면 지연 단축 여지 있음(오늘 검증한 3브랜치 동시실행과 같은 패턴, 아직 미적용).
9. gpt-4.1 교체 후 citation-check 재시도가 3회까지 자주 감(이번 실행: 총 63.9s 중 48s가 생성 단계, 그중 대부분이 재시도) — 큰 컨텍스트+DB 재무 원시 숫자(자릿수 긴 원단위 금액)가 늘어나면서 모델이 단위를 반올림/재표기하다 citation-check와 어긋나는 사례가 늘어난 것으로 보임, 프롬프트에 "DB 원본 숫자를 자릿수 그대로 인용" 지침 추가 등 재검토 필요. **→ [부분 조치, §7] 프롬프트 하드닝(단위 변환 금지) 추가 + gen_model 레버 제공. 재시도 근본 감소는 여러 질의 실측으로 재확인 필요.**
10. **[§7 후속] 생성 기본 모델 하향 결정**: Construct 1개 질의에선 gpt-4o-mini가 gpt-4.1 대비 2.2배 빠르고 동급 정확 — 여러 질의(정답 다양·큰 DB 컨텍스트 포함)로 확대 A/B 후 기본값을 gpt-4o-mini로 내릴지 결정(지금은 gen_model 레버만 제공, 기본 gpt-4.1 유지).
11. **[§7 후속] 검색 지연 추가 절감**: 키워드 포함 질의는 `weighted_hybrid`(0.05s)로도 도표3을 정확히 상위로 올림 — decompose(gpt-4o 분해 ≈2s)를 "순수 abstract일 때만" 타도록 게이팅하면 검색 지연을 더 줄일 여지(단 §5 abstract 우위는 유지해야 하므로 성격 분기 필요).
12. **✅ [해결됨, §9.2+§10.4] 표 브랜치 엔티티 갭**: 표 structured_metadata의 entities_mentioned가 빈값이라 표 속 종목이 엔티티로 안 남음(§8.4). image 브랜치가 커버해 recall엔 문제 없으나, 표 기반 필터/집계엔 갭 — `extract_table_metadata`가 entities_mentioned를 채우도록 점검 필요.
13. **✅ [해결됨, §12.1] OCR 텍스트레이어 대조 오탐률**: 수정(3)의 "원문에 없는 이미지 OCR 숫자 제거"가 반올림 표기차로 정상값을 지울 위험 — 여러 문서로 오탐/미탐률 측정 후 임계 조정.
14. **X [§9 후속] db_context 토큰 최적화**: 매칭 기업 전체 재무요약을 통짜로 프롬프트에 넣어 입력 6.3K토큰 중 ~2.1K가 DB컨텍스트 — 질의 관련 기업/핵심지표만 선별 주입하면 입력토큰·비용 대폭 절감(품질 영향 측정 필요).
15. **✅ [해결됨, §10.3] 생성 품질 판정 방법**: pairwise judge의 위치편향을 **RAGAS + 포인트와이즈 루브릭**으로 대체 — 결과 **gpt-4.1 우세**(faithfulness 0.893 vs 0.740, 논리 3.00 vs 2.22). → **남은과제 10(기본모델 하향)은 보류**가 결론. X 남은 리스크: N=9, 그리고 H4 문항이 낡은 인덱스 기준.
16. **✅ [해결됨, §10.1] 주간 수주 공시 표 미추출** — 탐지기 확장 + 컬럼헤더 회수 + 표 요약 청크로 K2/H4 모두 rank 1 복구. X 잔결함: TATR의 컬럼 중복 인식.
17. **✅ [해결됨, §10.4] 표 structured_metadata 컨텍스트 결핍** — 12번의 근본 원인이었음(프롬프트가 아니라 **입력에 회사명이 물리적으로 없었음**). 캡션/문서제목 주입으로 0% → 87%.
18. **X [§10.2 후속] 라우팅 재설계 필요**: 현행 라우팅이 고정 하이브리드보다 ndcg 0.21 낮고 지연 2~4배. 최소 조치는 **키워드 경로의 `fusion="rrf"` → 선형가중 0.7/0.3 교체**(`index_text.py:387`). 근본 조치는 (a) 분류기를 3타입(키워드/하이브리드/추상)으로 확장하거나, (b) **추상형에만 라우팅을 켜고 나머지는 고정 하이브리드 직행**. 단 §10.2 한계(인덱스 민감성) 때문에 문서 5~10건으로 재검증 후 반영할 것.
19. **X [§10.2 후속] 추상 경로 MQE 하위질의 품질**: MQE가 만든 하위질의가 지나치게 구체적이라 전략 서술 문단 대신 표로 검색이 쏠림(추상형 ndcg 0.455). 하위질의 프롬프트에 "서술/전망 문단도 겨냥" 지시 추가 검토(남은과제 7과 같은 뿌리일 가능성).
20. **X [운영] OpenAI 조직 쿼터 소진** — 2026-07-24 실험 중 `429 exceeded quota` 발생. 재측정 대기 항목: (a) 표 요약 청크 반영 후 RAGAS 재실행, (b) LLM 기반 검색 4방법(hyde/mqe/route/decompose)을 최종 인덱스에서 재측정(`results_fusion_routing_ab.json`의 해당 4행은 현재 `invalid` 표시, 유효 측정치는 같은 파일 `valid_prev_run`에 보존).
21. **X [§12.3 후속] citation-check 오탐**: 답변의 면책/메타 문장에 들어간 숫자("'150%' 같은 값은 사용하지 않았습니다")를 근거 미확인으로 잡는다 — 검사 대상에서 메타 문장을 제외하거나, 인용 형식 안의 숫자만 검사하도록 보완 필요.
22. **X [§12.2 후속] 차트 VLM 기본 on 전환 후 실지연 재측정**: 문서당 차트 수 × 15~22초가 실제 인제스트 지연에 얼마나 더해지는지 여러 문서로 측정하고, 필요하면 배치 추론 도입 검토(스레드 병렬화는 금지 — GPU 공유).
